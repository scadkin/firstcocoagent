# Scout Audit — 2026-04-15_0128
HEAD `339c92c` · S40 `629a2b5` (2026-04-02) · Mode: **venv** · Files audited **69/69** · Cost ~$5

_Scope: 69 files touched after S40 (`629a2b5`) — `agent/`, `tools/`, `scripts/`, `gas/Code.gs`. Python linters: ruff 0.15.10, pyflakes 3.4.0, bandit 1.9.4, vulture 2.16. LLM audit: Claude Sonnet 4.6 via Explore subagents, one file per call + 1 cross-file drift pass + 1 rule-compliance pass. Synthesis: Claude Sonnet 4.6. Verification: deterministic 100%-coverage byte-match._

## Executive Summary

- 270 total findings across the Scout codebase: 2 CRITICAL, 72 HIGH, 117 MEDIUM, 75 LOW, 4 NIT — the HIGH count alone represents a systemic quality gap that needs a dedicated remediation sprint.
- Correctness (108 findings) and Amnesia/drift (57+ findings) dominate, indicating that rapid feature development has outpaced regression-checking and that library utilities are being duplicated and diverged across scripts rather than reused.
- The two CRITICALs require immediate fixes before next deploy: a guaranteed NameError that crashes the /unanswered Telegram command, and a hardcoded placeholder secret token in gas/Code.gs that nullifies authentication if not substituted.
- Top 3 actions for Steven: (1) Fix the `gas` NameError in agent/main.py:1775 — any /unanswered invocation will crash. (2) Add a runtime guard in gas/Code.gs rejecting the placeholder SECRET_TOKEN. (3) Audit and consolidate the three divergent classify_role implementations in scripts/ against tools/role_classifier.py to eliminate silent misclassification.

## Top 10 Actions

1. **`fix-now`** — [CRIT-1] F821: `gas` undefined in /unanswered handler — NameError at runtime (`agent/main.py:1775`) — Any invocation of /unanswered raises NameError and crashes the Telegram handler, making this a live production outage for that command.
2. **`fix-now`** — [CRIT-2] Hardcoded placeholder secret token ships as literal default (`gas/Code.gs:22`) — Add a startup guard in gas/Code.gs that throws if SECRET_TOKEN equals the placeholder string, preventing accidental open-auth deploys.
3. **`fix-now`** — [HIGH-4] `/signal_compliance` and `/scan_compliance` match two separate `elif` branches — one is silently dead (`agent/main.py:3567`) — The dead elif branch means /scan_compliance silently runs the wrong code path with different argument counts, producing incorrect compliance scan results.
4. **`fix-now`** — [HIGH-3] `/signals new` calls `format_hot_signals` synchronously instead of awaiting it (`agent/main.py:3015`) — The /signals new branch both blocks the event loop and ignores the filtered signal list, meaning it never actually shows only new signals as intended.
5. **`fix-now`** — [HIGH-5] createDraft reply path passes empty string as body to createDraftReply (`gas/Code.gs:189`) — Every plain-text draft created via the reply path is silently empty because the body is passed to an unrecognized options key instead of the required first positional argument.
6. **`fix-now`** — [HIGH-9] classify_role reimplemented three times in scripts/ with divergent bucket names vs. tools/role_classifier.py (`scripts/audit_c4_prospects.py:74`) — Three divergent classify_role implementations produce incompatible bucket names that silently corrupt role-based filtering and reporting across scripts.
7. **`fix-now`** — [HIGH-13] IndexError when dn_idx is -1 in archdiocese loop (`scripts/bug5_phase0_scan.py:39`) — The -1 sentinel guard failure causes row[-1] to be used as district name data silently across multiple loops in bug5_phase0_scan.py, corrupting scan results.
8. **`investigate`** — [HIGH-11] Off-by-one in row index — 1-indexed display vs 0-indexed Sheets API startIndex (`scripts/bug5_cleanup_lackland_test.py:40`) — The off-by-one index in bug5_cleanup_lackland_test.py may have already deleted wrong rows in prior runs; verify sheet state before re-running the cleanup script.
9. **`fix-now`** — [HIGH-8] Cost ceiling check uses v1 cost alone but docstring says 'combined v1+v2 cost' (`scripts/ab_research_engine.py:324`) — The cost ceiling allowing up to double the stated limit creates real financial exposure; fix the guard to compare v1_cost + v2_estimated_cost against the ceiling.
10. **`fix-now`** — [HIGH-2] Cross-contamination variable `cross` is read from the import result but silently discarded (`agent/main.py:1307`) — Cross-contamination events during SF Contacts import are invisible to the operator because the cross counter is extracted but never surfaced in the summary message.

## Themes

- Undefined or uninitialized variables passed to async executor calls in agent/main.py (CRIT-1 and the /signals new branch pattern), indicating that new command branches are added without tracing variable scope.
- Duplicate logic in scripts/ that diverges silently from canonical tools/ implementations — classify_role appears three times with incompatible output schemas, and similar patterns likely exist for other utilities.
- Sentinel value mishandling: the -1 column-not-found sentinel is used as a real list index in multiple scripts/bug5_* files because guards check length but not negativity.
- GAS API misuse recurring across gas/Code.gs — wrong positional argument passing, hardcoded slide counts, and a statusCode parameter the platform cannot honor.
- Silent data loss through broad except-and-continue patterns: KeyError, missing dict keys, and cross-contamination counters are all swallowed and replaced with vague user-facing messages rather than propagating structured error details.
- Cost and resource ceiling logic implemented against single-phase measurements when the documented contract requires multi-phase aggregation, creating systematic overspend exposure in research scripts.

## Critical (1)

### [CRIT-2] Hardcoded placeholder secret token ships as literal default ⭐
- **File:** `gas/Code.gs:22` @ `339c92c`
- **Category:** Security
- **Introduced in:** `863facc` (no session tag)
- **Evidence:**
  ```python
  var SECRET_TOKEN = "REPLACE_WITH_YOUR_SECRET_TOKEN_HERE";
  ```
- **Why it matters:** If deployed without substituting the token, every caller who sends the placeholder string is authorized. No runtime guard rejects the placeholder value.
- **Suggested fix:** Add guard in doPost that rejects the placeholder string.
- **Recommended action:** `fix-now` — Add a startup guard in gas/Code.gs that throws if SECRET_TOKEN equals the placeholder string, preventing accidental open-auth deploys.
- **Confidence:** high
- **Re-eval:** defensible on re-read — The literal string 'REPLACE_WITH_YOUR_SECRET_TOKEN_HERE' is the default value with no runtime guard rejecting it, so any caller who sends that string is authenticated. The security concern is real, though severity depends on whether the GAS deployment is public-facing.

## High (68)

### `agent/main.py` (post-S40 commits: 49)

### [HIGH-1] Sequence doc write failure path swallows `seq_result["success"]` KeyError if key is absent ⭐
- **File:** `agent/main.py:494` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `148b29c` (no session tag)
- **Evidence:**
  ```python
          if seq_result["success"]:
              gas = get_gas_bridge()
              if gas:
                  doc_result = sequence_builder.write_sequence_to_doc(campaign_name, seq_result["steps"], gas)
  ```
- **Why it matters:** In `_on_prospect_research_complete`, `seq_result["success"]` and `seq_result["steps"]` are accessed with direct key indexing (not `.get()`). If `sequence_builder.build_sequence` returns a dict without these keys, this raises `KeyError` which is caught by the outer `except Exception as e` and surfaces only as a vague Telegram warning, losing the real root cause.
- **Suggested fix:** Use `seq_result.get("success")` and `seq_result.get("steps", [])`, matching the defensive pattern used in the `build_sequence` tool handler at lines 862-863.
- **Confidence:** medium
- **Re-eval:** defensible on re-read — Direct key indexing on an unvalidated return dict from sequence_builder.build_sequence without `.get()` will raise KeyError if the schema shifts, and the outer except swallows it — this is a real but common Python sloppiness pattern that warrants a fix.

### [HIGH-2] Cross-contamination variable `cross` is read from the import result but silently discarded ⭐
- **File:** `agent/main.py:1307` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `c1377ab` (no session tag)
- **Evidence:**
  ```python
          cross = result.get("cross_checked", 0)
  ```
- **Why it matters:** Both the SF Leads (line 1258) and SF Contacts (line 1307) import paths extract `cross` from the result dict but never use it in the summary message or logs. The sessions 50-58 bug-hunt cycle specifically tracked cross-contamination; silently dropping the counter means cross-contamination events during contacts import are invisible to the operator.
- **Suggested fix:** Include `cross_checked` in the Telegram summary message for both import paths (e.g. `• {cross} cross-checked against active accounts`), matching the spirit of the bug-5 cross-contamination tracking added in commit `22dc28b`.
- **Recommended action:** `fix-now` — Cross-contamination events during SF Contacts import are invisible to the operator because the cross counter is extracted but never surfaced in the summary message.
- **Confidence:** high
- **Re-eval:** defensible on re-read — The `cross` variable is extracted but not surfaced in the operator summary message, which is a genuine observability gap given that cross-contamination tracking was an explicit bug-hunt priority in prior sessions.

### [HIGH-3] `/signals new` calls `format_hot_signals` synchronously instead of awaiting it ⭐
- **File:** `agent/main.py:3015` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `7c2b7f8` (no session tag)
- **Evidence:**
  ```python
              elif args.lower() == "new":
                  sigs = await loop.run_in_executor(
                      None, signal_processor.get_active_signals, "", "district", "new")
                  output = signal_processor.format_hot_signals(20, "") if sigs else "No new signals."
  ```
- **Why it matters:** `format_hot_signals` is called directly (not via `run_in_executor`) in the `"new"` branch while every other branch in the same `if/elif` uses `await loop.run_in_executor(...)`. If `format_hot_signals` does any blocking I/O (sheet reads) this blocks the event loop; more importantly, it ignores the `sigs` filter and always formats all signals, defeating the "show only new" intent.
- **Suggested fix:** Wrap the call in `await loop.run_in_executor(None, signal_processor.format_hot_signals, 20, "")` and pass the pre-filtered `sigs` list, or call a formatter that accepts the already-fetched list directly.
- **Recommended action:** `fix-now` — The /signals new branch both blocks the event loop and ignores the filtered signal list, meaning it never actually shows only new signals as intended.
- **Confidence:** high
- **Re-eval:** defensible on re-read — The `format_hot_signals` call in the 'new' branch is missing `run_in_executor` wrapping and also ignores the `sigs` result, passing hardcoded empty-string filters — both the blocking I/O risk and the logic defect are real.

### [HIGH-4] `/signal_compliance` and `/scan_compliance` match two separate `elif` branches — one is silently dead ⭐
- **File:** `agent/main.py:3567` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `442d7cb` (no session tag)
- **Evidence:**
  ```python
      elif user_text.lower().startswith(("/scan_compliance", "/compliance_gap", "scan compliance")):
  ```
- **Why it matters:** At line 3567 a branch handles `/scan_compliance` (and `/compliance_gap`). At line 3689, a later branch also matches `/signal_compliance` AND `/scan_compliance`. Because Python evaluates `elif` chains top-down, the line-3689 branch for `/scan_compliance` is unreachable — the line-3567 branch always fires first. The two branches call `compliance_gap_scanner.scan_compliance_gaps` with different argument counts, so one path silently omits the cap.
- **Suggested fix:** Deduplicate the routing: give `/scan_compliance` and `/signal_compliance` distinct trigger strings, or merge both branches into one handler that decides behavior based on which prefix matched.
- **Recommended action:** `fix-now` — The dead elif branch means /scan_compliance silently runs the wrong code path with different argument counts, producing incorrect compliance scan results.
- **Confidence:** high
- **Re-eval:** defensible on re-read — Python elif chains are evaluated top-down, so the line-3689 branch for /scan_compliance is genuinely unreachable when the line-3567 branch matches first; the different argument counts make this a correctness bug, not just dead code.

### `tools/signal_processor.py` (post-S40 commits: 40)

### [HIGH-71] Superintendent directory updated with unfiltered (non-territory) Claude items ⭐
- **File:** `tools/signal_processor.py:2440` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `a974ee7` (no session tag)
- **Evidence:**
  ```python
          dir_result = update_superintendent_directory(items)
  ```
- **Why it matters:** At line 2440, `items` is the raw Claude-extracted list before the territory filter at line 2386-2387. Districts from non-territory states (filtered out of `signals`) are still persisted to the superintendent GitHub directory, polluting it with out-of-territory leadership data.
- **Suggested fix:** Pass the territory-filtered list to `update_superintendent_directory`. Build a separate filtered items list and call `update_superintendent_directory(territory_items)`.
- **Confidence:** high

### `tools/district_prospector.py` (post-S40 commits: 25)

### [HIGH-60] clear_by_strategy reads only 19 columns (A:S) but schema is 20 — Notes column silently dropped on rewrite ⭐
- **File:** `tools/district_prospector.py:307` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `4af201d` (no session tag)
- **Evidence:**
  ```python
          range=f"'{TAB_PROSPECT_QUEUE}'!A:S"
      ).execute()
      rows = result.get("values", [])
      if len(rows) < 2:
          return {"cleared": 0, "total_before": 0, "total_after": 0}
  
      headers = rows[0]
      strategy_idx = headers.index("Strategy") if "Strategy" in headers else 5
  ```
- **Why it matters:** The Prospecting Queue has 20 columns (A through T); reading only A:S caps every row at 19 values. When surviving rows are written back via `kept_rows.append(row)` (line 325), the 20th column (Notes, column T) is absent. The subsequent clear+rewrite at line 328-339 permanently erases Notes for every row that passes the strategy filter.
- **Suggested fix:** Change the read range from `A:S` to `A:T` so all 20 columns are fetched. Also use `padded` (not raw `row`) when building kept_rows to ensure uniform column count on rewrite.
- **Confidence:** high

### [HIGH-61] scan_sequence_for_reengagement silently drops all CA prospects — _TERRITORY_STATES missing CA ⭐
- **File:** `tools/district_prospector.py:3415` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `39adfd5` (no session tag)
- **Evidence:**
  ```python
              # Must be in territory — skip if unknown or out-of-territory
              if not state_code or state_code not in _TERRITORY_STATES:
                  continue
  ```
- **Why it matters:** The module-level `_TERRITORY_STATES` (line 87) does not include 'CA', but California is a valid territory state: `suggest_cold_license_requests` uses a local `territory_states` set that explicitly includes 'CA' (line 1893). Any prospect whose district resolves to CA will be excluded by `scan_sequence_for_reengagement`.
- **Suggested fix:** Add 'CA' to the module-level `_TERRITORY_STATES` set so it is the single source of truth used consistently across all scanning functions.
- **Confidence:** high

### `tools/outreach_client.py` (post-S40 commits: 11)

### [HIGH-63] verify_sequence returns passed=False instead of passed=None on transient template-fetch error ⭐
- **File:** `tools/outreach_client.py:1172` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `1f22991` (no session tag)
- **Evidence:**
  ```python
          "passed": len(failures) == 0 and not errors,
  ```
- **Why it matters:** The docstring contracts `passed=None` for 'network error' cases, and the early-exit paths correctly return `None`. But the final return uses `and not errors`, so when `_try_fetch` appends to `errors` for a per-step template failure, `passed` becomes `False` — not `None`. The caller in `create_sequence` treats `False` as a hard verification failure and surfaces a misleading empty-failures 'POST-WRITE verification FAILED' log on an otherwise valid sequence.
- **Suggested fix:** Change the final return to `'passed': None if errors else len(failures) == 0` to honour the `bool | None` contract.
- **Confidence:** high

### [HIGH-64] create_sequence omits owner_id from verify_sequence expected dict, causing false owner mismatch if OUTREACH_OWNER_USER_ID is set ⭐
- **File:** `tools/outreach_client.py:1464` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `1f22991` (no session tag)
- **Evidence:**
  ```python
          expected = {
              "schedule_id": schedule_id,
              "allow_no_schedule": allow_no_schedule,
              "step_count": len(steps),
              "step_interval_ranges": step_interval_ranges,
              "meeting_link": meeting_link,
              "require_cc_schools_link": require_cc_schools_link,
          }
  ```
- **Why it matters:** create_sequence resolves owner from `os.environ.get('OUTREACH_OWNER_USER_ID') or _user_id or '11'` (line 1302) and writes that to Outreach. But the `expected` dict passed to `verify_sequence` never includes `owner_id`, so verify_sequence defaults to checking `11`. If the env var is set to a different user ID, create_sequence writes the correct owner but verify_sequence flags it as a mismatch.
- **Suggested fix:** Add `'owner_id': owner_user_id_int` to the `expected` dict so the post-write check verifies against the ID that was actually written.
- **Confidence:** high

### `tools/research_engine.py` (post-S40 commits: 9)

### [HIGH-69] Stale `email` snapshot allows cross-district-dropped email to pass VERIFIED upgrade ⭐
- **File:** `tools/research_engine.py:909` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `dbcadc2` (no session tag)
- **Evidence:**
  ```python
              email = c.get("email", "").lower().strip()
  
              # Primary dedup key: name + district
              key = f"{fn}|{ln}|{self.district_name.lower()}"
  ```
- **Why it matters:** The local variable `email` is snapshotted from `c['email']` at line 909. If the cross-district check at line 934 clears `c['email'] = ""`, `email` still holds the original value. Line 972 then checks `if email and email in [e.lower() for e in self.known_emails]` — because `email` is still truthy, it unconditionally sets `c['email_confidence'] = 'VERIFIED'`, overriding the cross-district drop.
- **Suggested fix:** After any mutation that clears `c['email']`, re-read from the dict before the known_emails upgrade check: `email = c.get('email', '')`.
- **Confidence:** high

### `gas/Code.gs` (post-S40 commits: 5)

### [HIGH-5] createDraft reply path passes empty string as body to createDraftReply ⭐
- **File:** `gas/Code.gs:189` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `a747344` (no session tag)
- **Evidence:**
  ```python
      draft = thread.createDraftReply("", {
        to: to,
        subject: subject,
        cc: cc || undefined,
        htmlBody: (contentType === "text/html") ? body : undefined,
        body: (contentType !== "text/html") ? body : undefined
      });
  ```
- **Why it matters:** First positional arg to createDraftReply is plain-text body. Passing empty string and setting options.body means plain-text drafts are always blank (options.body is not a recognized option).
- **Suggested fix:** Pass body as first argument: thread.createDraftReply(body, options).
- **Recommended action:** `fix-now` — Every plain-text draft created via the reply path is silently empty because the body is passed to an unrecognized options key instead of the required first positional argument.
- **Confidence:** high
- **Re-eval:** defensible on re-read — Passing an empty string as the first positional argument to createDraftReply while placing the intended body in an unrecognized options key means plain-text drafts will always be empty — this is a clear API misuse bug.

### [HIGH-6] createDistrictDeck slide_count return value hardcoded after saveAndClose ⭐
- **File:** `gas/Code.gs:595` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `863facc` (no session tag)
- **Evidence:**
  ```python
    pres.saveAndClose();
  
    return {
      success: true,
      presentation_id: pres.getId(),
      url: "https://docs.google.com/presentation/d/" + pres.getId() + "/edit",
      title: deckTitle,
      slide_count: 5 + (caseStudy ? 1 : 0)
    };
  ```
- **Why it matters:** pres.getId() called after saveAndClose may throw or return stale data. slide_count is hardcoded formula rather than pres.getSlides().length.
- **Suggested fix:** Capture pres.getId() and pres.getSlides().length into local variables before saveAndClose.
- **Confidence:** medium

### [HIGH-7] jsonResponse ignores statusCode — always returns HTTP 200 ⭐
- **File:** `gas/Code.gs:611` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `863facc` (no session tag)
- **Evidence:**
  ```python
  function jsonResponse(data, statusCode) {
    return ContentService
      .createTextOutput(JSON.stringify(data))
      .setMimeType(ContentService.MimeType.JSON);
  }
  ```
- **Why it matters:** statusCode parameter accepted but never used. GAS ContentService doesn't support setting HTTP status codes. Unauthorized response and error response both return HTTP 200.
- **Suggested fix:** Remove statusCode parameter; ensure Python-side callers gate on JSON success field.
- **Confidence:** high
- **Re-eval:** defensible on re-read — GAS ContentService does not support setting HTTP status codes, so the statusCode parameter is structurally ignored; however this is a GAS platform limitation, not a code logic error, and callers parsing the JSON body for success/error fields still function correctly.

### `tools/gas_bridge.py` (post-S40 commits: 5)

### [HIGH-62] create_google_doc calls GAS action 'createGoogleDoc' (camelCase) while all other actions use snake_case ⭐
- **File:** `tools/gas_bridge.py:356` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `6add3d8` (no session tag)
- **Evidence:**
  ```python
          result = self._call("createGoogleDoc", {
  ```
- **Why it matters:** Every other _call invocation uses snake_case action strings. If GAS Code.gs dispatcher uses snake_case for this action, the call will hit unknown-action branch and silently break pre-call brief creation.
- **Suggested fix:** Verify action string Code.gs expects; most likely should be 'create_google_doc'.
- **Confidence:** medium

### `scripts/rule_scanner.py` (post-S40 commits: 4)

### [HIGH-39] R21 pre_filter uses normalized text but R21 check uses raw text for anchors ⭐
- **File:** `scripts/rule_scanner.py:238` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `0b72295` (no session tag)
- **Evidence:**
  ```python
          if pre_filter and not re.search(pre_filter, normalized, flags=re.IGNORECASE):
              continue
  ```
- **Why it matters:** R21's anchor check uses raw text (anchor_text_scope='raw') but pre_filter checks normalized text. Asymmetric — triggering phrase inside fenced code block would not fire R21 if pre_filter doesn't match on normalized text.
- **Suggested fix:** Apply pre_filter against the same text used for trigger matching, or document the split clearly.
- **Confidence:** medium

### `tools/proximity_engine.py` (post-S40 commits: 4)

### [HIGH-67] add_proximity_prospects writes duplicates — no pre-flight dedup against existing queue rows ⭐
- **File:** `tools/proximity_engine.py:399` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `42e18ae` (no session tag)
- **Evidence:**
  ```python
      rows = []
      for d in districts:
          nearest = d.get("nearest_account", reference_account) or reference_account
          priority = district_prospector._calculate_priority(
              "proximity", 0, 0, d.get("enrollment", 0),
              distance_miles=d.get("distance_miles", 50),
          )
  ```
- **Why it matters:** Every invocation appends all passed districts; running 'add nearby' twice writes identical rows to the Prospecting Queue.
- **Suggested fix:** Filter out name_keys already in the queue via get_all_prospects().
- **Confidence:** high

### [HIGH-68] county_to_esa dict silently drops ESAs when multiple ESAs share the same county code ⭐
- **File:** `tools/proximity_engine.py:555` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `42e18ae` (no session tag)
- **Evidence:**
  ```python
      county_to_esa = {}
      for esa in esas:
          cc = esa.get("county_code", "")
          if cc:
              county_to_esa[cc] = esa["name"]
  ```
- **Why it matters:** If two ESAs exist in the same county, the second write silently overwrites the first. No collision detection or warning.
- **Suggested fix:** Build county_to_esa as a dict of lists, or log warnings on collision.
- **Confidence:** medium

### `tools/territory_map.py` (post-S40 commits: 4)

### [HIGH-72] _find_district_coords called in O(N*M) loop with candidate_map rebuild per call ⭐
- **File:** `tools/territory_map.py:198` @ `339c92c`
- **Category:** Performance red flags
- **Introduced in:** `05f2d80` (no session tag)
- **Evidence:**
  ```python
          matched = _find_district_coords(districts, name_key)
          if not matched:
              continue
  ```
- **Why it matters:** Full linear scan plus candidate_map rebuild for each prospect/pipeline opp/active account. O(N*M) per layer.
- **Suggested fix:** Pre-build dict keyed by Name Key once before the loops.
- **Confidence:** high

### `scripts/s59_regen_diocesan_sequences.py` (post-S40 commits: 3)

### [HIGH-41] Token written back to repo file non-atomically — corrupt token state if write interrupted ⭐
- **File:** `scripts/s59_regen_diocesan_sequences.py:40` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `7c162b6` (no session tag)
- **Evidence:**
  ```python
      with open('memory/outreach_tokens.json', 'w') as _f:
          json.dump({
              'access_token': outreach_client._access_token,
              'refresh_token': outreach_client._refresh_token,
              'expires_at': outreach_client._token_expires_at,
              'user_id': outreach_client._user_id,
          }, _f, indent=2)
  ```
- **Why it matters:** File opened with 'w' (truncates immediately) before json.dump completes. Kill mid-write = empty token file, locking operator out. No validation that new tokens are non-empty before persisting.
- **Suggested fix:** Write to temp file in same dir then os.replace() atomically; assert tokens non-empty before persisting.
- **Confidence:** high

### [HIGH-42] day_to_interval_seconds step 1 hardcoded to 300 regardless of input ⭐
- **File:** `scripts/s59_regen_diocesan_sequences.py:217` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `880d77b` (no session tag)
- **Evidence:**
  ```python
          interval_seconds = day_to_interval_seconds(s['day'], prev_day if i > 0 else 0)
  ```
- **Why it matters:** For i==0 function returns 300 (5-min hardcoded) regardless of s['day']. If builder returns non-zero step-1 day, silently ignored.
- **Suggested fix:** Document or assert steps[0]['day']==0, or handle the day==0 sentinel explicitly.
- **Confidence:** medium

### `scripts/test_round1_unit.py` (post-S40 commits: 3)

### [HIGH-49] _run_async dead-code branch never reachable ⭐
- **File:** `scripts/test_round1_unit.py:163` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `bd7a562` (no session tag)
- **Evidence:**
  ```python
  def _run_async(coro):
      import asyncio
      return asyncio.get_event_loop().run_until_complete(coro) if False else asyncio.run(coro)
  ```
- **Why it matters:** The 'if False' branch is unreachable dead code. asyncio.run() creates a new event loop each call; if invoked from inside a running loop (pytest-asyncio), raises RuntimeError.
- **Suggested fix:** Remove dead 'if False' branch. Use nest_asyncio or detect running loop if needed.
- **Confidence:** high

### [HIGH-50] Test hardcodes model name that may drift ⭐
- **File:** `scripts/test_round1_unit.py:331` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `bd7a562` (no session tag)
- **Evidence:**
  ```python
      assert records[0]["model"] == "claude-sonnet-4-6"
  ```
- **Why it matters:** Hardcoded model string will fail silently in CI when model constant bumps, masking version drift.
- **Suggested fix:** Import model constant from tools.contact_extractor and assert against symbol.
- **Confidence:** medium

### `tools/activity_tracker.py` (post-S40 commits: 3)

### [HIGH-55] log_activity inside sync_gmail_activities re-runs _ensure_tabs per activity ⭐
- **File:** `tools/activity_tracker.py:574` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `a2ef953` (no session tag)
- **Evidence:**
  ```python
              log_activity(
                  activity_type=act["activity_type"],
                  district=act["district"],
                  contact=act["contact"],
                  notes=act["notes"],
                  source=act["source"],
                  message_id=mid,
              )
  ```
- **Why it matters:** log_activity always calls _ensure_tabs() and _get_service() internally. Sync loop triggers extra spreadsheets().get() and build() per iteration — 20 new events = 20 extra network calls.
- **Suggested fix:** Use lower-level append helper or batch all new rows.
- **Confidence:** high

### [HIGH-56] Timezone-naive/aware comparison mutates loop variable sent_dt ⭐
- **File:** `tools/activity_tracker.py:844` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `a974ee7` (no session tag)
- **Evidence:**
  ```python
              except TypeError:
                  if reply_dt.tzinfo is None and sent_dt.tzinfo is not None:
                      reply_dt = reply_dt.replace(tzinfo=sent_dt.tzinfo)
                  elif sent_dt.tzinfo is None and reply_dt.tzinfo is not None:
                      sent_dt = sent_dt.replace(tzinfo=reply_dt.tzinfo)
                  if reply_dt > sent_dt:
                      replied = True
                      break
  ```
- **Why it matters:** sent_dt mutation via replace() corrupts value used later at line 855 (cutoff - sent_dt) and subsequent reply candidates in the inner loop.
- **Suggested fix:** Use local shadow variables _reply_dt/_sent_dt inside the except block.
- **Confidence:** high

### `tools/sheets_writer.py` (post-S40 commits: 3)

### [HIGH-70] count_leads dead loop with wasted API calls ⭐
- **File:** `tools/sheets_writer.py:696` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `48d6cd7` (no session tag)
- **Evidence:**
  ```python
          for tab, key in [(TAB_LEADS, "leads"), (TAB_NO_EMAIL, "no_email")]:
              result = service.spreadsheets().values().get(
                  spreadsheetId=sheet_id,
                  range=f"'{tab}'!A:A"
              ).execute()
              rows = result.get("values", [])
              counts = {
                  "leads": max(0, len(rows) - 1),  # subtract header
              }
  ```
- **Why it matters:** For-loop overwrites `counts` each iteration with hardcoded 'leads' key, discarding the key variable. Loop result thrown away entirely because two fresh API calls are made below — 2 of 4 API calls wasted.
- **Suggested fix:** Remove the dead for-loop entirely.
- **Confidence:** high

### `scripts/diocesan_drip.py` (post-S40 commits: 2)

### [HIGH-20] cmd_verify returns 0 even on live-count errors ⭐
- **File:** `scripts/diocesan_drip.py:399` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
          except Exception as e:
              live_count = -1
              logger.warning(f"seq {sid} live count error: {e}")
  ```
- **Why it matters:** When get_sequence_states raises, live_count=-1 and match=(live_count>=exp) is True only if exp<=0. Error only logged, exit code stays 0.
- **Suggested fix:** Set error flag, mark match False unconditionally, ensure all_match becomes False.
- **Confidence:** high

### `scripts/fetch_csta_roster.py` (post-S40 commits: 2)

### [HIGH-27] serper_search silently swallows HTTP errors ⭐
- **File:** `scripts/fetch_csta_roster.py:198` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `69ec3b8` (no session tag)
- **Evidence:**
  ```python
  def serper_search(query: str, num: int = 10) -> list[dict]:
      r = httpx.post(
          SERPER_URL,
          headers={"X-API-Key": SERPER_KEY, "Content-Type": "application/json"},
          json={"q": query, "num": num},
          timeout=20.0,
      )
      return r.json().get("organic", [])
  ```
- **Why it matters:** r.raise_for_status() never called. 4xx/5xx responses silently return empty list or cause opaque JSON parse errors.
- **Suggested fix:** Call r.raise_for_status() before r.json().
- **Confidence:** high

### [HIGH-28] Haiku model name uses non-existent model ID ⭐
- **File:** `scripts/fetch_csta_roster.py:416` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `69ec3b8` (no session tag)
- **Evidence:**
  ```python
          model="claude-haiku-4-5-20251001",
  ```
- **Why it matters:** Model string may not match any known Anthropic model ID. Same at line 568. If wrong, every API call fails with 400.
- **Suggested fix:** Verify exact model string via Anthropic API or docs.
- **Confidence:** medium

### `scripts/load_campaign.py` (post-S40 commits: 2)

### [HIGH-29] execute_load_plan called with dry_run=False hardcoded; --dry-run flag not forwarded ⭐
- **File:** `scripts/load_campaign.py:520` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `09668c0` (no session tag)
- **Evidence:**
  ```python
      summary = execute_load_plan(
          plans,
          state_path=str(state_path(campaign.slug).with_suffix(".load.json")),
          audit_path=str(audit_path(campaign.slug)),
          target_day=target_day,
          sleep_seconds=campaign.sleep_seconds,
          verify_sequence_active=True,
          dry_run=False,
      )
  ```
- **Why it matters:** dry_run hardcoded. Early-return guard above protects but if control flow changes, real Outreach writes occur despite --dry-run.
- **Suggested fix:** Replace dry_run=False with dry_run=dry_run.
- **Confidence:** medium

### `tools/csv_importer.py` (post-S40 commits: 2)

### [HIGH-57] import_accounts returns non-zero imported count even when sheet write fails ⭐
- **File:** `tools/csv_importer.py:547` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `a2ef953` (no session tag)
- **Evidence:**
  ```python
      except Exception as e:
          errors.append(f"Sheet write failed: {e}")
          logger.error(f"import_accounts sheet write error: {e}")
  
      return {
          "imported":   imported,
  ```
- **Why it matters:** imported counter incremented before try block. If write raises, function still returns imported:N with errors list, misleading caller.
- **Suggested fix:** Reset imported to 0 in except handler.
- **Confidence:** high

### [HIGH-58] merge_accounts and replace_accounts_by_state return misleading counts on sheet write failure ⭐
- **File:** `tools/csv_importer.py:690` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `baf004f` (no session tag)
- **Evidence:**
  ```python
      except Exception as e:
          errors.append(f"Sheet write failed: {e}")
          logger.error(f"merge_accounts sheet write error: {e}")
  
      return {
          "imported":   updated + added,
  ```
- **Why it matters:** Same pattern as import_accounts. Telegram handler displays counts as success.
- **Suggested fix:** Zero out counters in exception path.
- **Confidence:** high

### `scripts/ab_research_engine.py` (post-S40 commits: 1)

### [HIGH-8] Cost ceiling check uses v1 cost alone but docstring says 'combined v1+v2 cost' ⭐
- **File:** `scripts/ab_research_engine.py:324` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `21d9b3b` (no session tag)
- **Evidence:**
  ```python
      if v1_summary["cost_total_usd"] > args.max_cost_usd:
          record["cost_ceiling_hit_after_v1"] = True
          record["v2"] = {"status": "skipped_cost_ceiling"}
  ```
- **Why it matters:** Help text says 'ceiling for combined v1+v2' but guard compares v1 alone. Ceiling of $5 allows combined spend up to ~$10.
- **Suggested fix:** Either halve ceiling for pre-v2 check or update help text to reflect per-arm semantics.
- **Recommended action:** `fix-now` — The cost ceiling allowing up to double the stated limit creates real financial exposure; fix the guard to compare v1_cost + v2_estimated_cost against the ceiling.
- **Confidence:** high
- **Re-eval:** defensible on re-read — The cost ceiling guard compares only v1 cost against a limit documented as a combined v1+v2 ceiling, allowing up to double the intended spend — the claim is accurate and the financial risk is real.

### `scripts/audit_leads_cross_contamination.py` (post-S40 commits: 1)

### [HIGH-10] Sheet range hard-capped at K5000 may silently truncate ⭐
- **File:** `scripts/audit_leads_cross_contamination.py:145` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
          spreadsheetId=sheet_id, range="'Leads from Research'!A1:K5000"
  ```
- **Why it matters:** Fixed 5000 row cap silently drops data beyond that row with no warning.
- **Suggested fix:** Use unbounded range A:K or assert row count < 4990.
- **Confidence:** high

### `scripts/bug5_cleanup_lackland_test.py` (post-S40 commits: 1)

### [HIGH-11] Off-by-one in row index — 1-indexed display vs 0-indexed Sheets API startIndex ⭐
- **File:** `scripts/bug5_cleanup_lackland_test.py:40` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
  lackland_rows = [i + 1 for i, row in enumerate(values[1:], start=1)
                   if dn_idx < len(row) and "lackland" in row[dn_idx].lower()]
  ```
- **Why it matters:** enumerate(values[1:], start=1) + i+1 gives 2-based indices. Sheets deleteDimension uses 0-based startIndex. Delete loop targets rows one position below intended.
- **Suggested fix:** Collect 0-based indices or subtract 1 at delete step.
- **Recommended action:** `investigate` — The off-by-one index in bug5_cleanup_lackland_test.py may have already deleted wrong rows in prior runs; verify sheet state before re-running the cleanup script.
- **Confidence:** high
- **Re-eval:** defensible on re-read — The double-offset from enumerate(values[1:], start=1) plus i+1 produces 2-based indices while the Sheets API deleteDimension uses 0-based startIndex, meaning delete operations target one row below the intended row — a genuine data-destruction bug in cleanup scripts.

### `scripts/bug5_dryrun.py` (post-S40 commits: 1)

### [HIGH-12] run_live_mode hard-gate message is partly dead code ⭐
- **File:** `scripts/bug5_dryrun.py:193` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `148aca6` (no session tag)
- **Evidence:**
  ```python
      if missing_good or surviving_bad or clean_fails:
          sys.exit("LIVE MODE HARD GATE FAILED")
  ```
- **Why it matters:** Earlier sys.exit(1) for clean_fails means line 193's combined gate never fires for that branch. Misleading about coverage.
- **Suggested fix:** Remove early sys.exit and let combined gate handle all three conditions uniformly.
- **Confidence:** medium

### `scripts/bug5_phase0_scan.py` (post-S40 commits: 1)

### [HIGH-13] IndexError when dn_idx is -1 in archdiocese loop ⭐
- **File:** `scripts/bug5_phase0_scan.py:39` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
      dn = row[dn_idx] if dn_idx < len(row) else ""
  ```
- **Why it matters:** When column not found, dn_idx=-1. Guard 'dn_idx < len(row)' is satisfied for any non-empty row, so row[-1] returns last column silently. Same pattern at 71, 93, 94, 95.
- **Suggested fix:** Add 'dn_idx != -1 and' to guards.
- **Recommended action:** `fix-now` — The -1 sentinel guard failure causes row[-1] to be used as district name data silently across multiple loops in bug5_phase0_scan.py, corrupting scan results.
- **Confidence:** high
- **Re-eval:** defensible on re-read — The guard `dn_idx < len(row)` does not handle the -1 sentinel case, so row[-1] is silently returned as the district name column value — this is a real off-by-one logic error that will produce wrong data, not a crash.

### [HIGH-14] Same -1 sentinel bug for email_idx and src_idx ⭐
- **File:** `scripts/bug5_phase0_scan.py:94` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
      email = (row[email_idx] if email_idx < len(row) else "").lower()
      src = (row[src_idx] if src_idx < len(row) else "").lower()
  ```
- **Why it matters:** Same pattern as line 39. row[-1] silently returned as email or src URL.
- **Suggested fix:** Apply same -1 guard.
- **Confidence:** high

### `scripts/bug5_smoke_verify.py` (post-S40 commits: 1)

### [HIGH-15] KeyError crash when expected column is absent from lead sheet headers ⭐
- **File:** `scripts/bug5_smoke_verify.py:68` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
  dn_idx = lead_headers.index("District Name")
  date_idx = lead_headers.index("Date Found")
  email_idx = lead_headers.index("Email")
  src_idx = lead_headers.index("Source URL")
  ```
- **Why it matters:** list.index() raises ValueError on missing column with no actionable message.
- **Suggested fix:** Wrap each in try/except ValueError with descriptive error.
- **Confidence:** high

### `scripts/create_c4_sequences.py` (post-S40 commits: 1)

### [HIGH-16] get_access_token swallows HTTP errors from GitHub token fetch ⭐
- **File:** `scripts/create_c4_sequences.py:32` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
      resp = httpx.get(
          "https://api.github.com/repos/scadkin/firstcocoagent/contents/memory/outreach_tokens.json",
          headers={"Authorization": f"token {gh_token}", "Accept": "application/vnd.github.v3.raw"},
          timeout=15.0,
      )
      tokens = json.loads(resp.text)
  ```
- **Why it matters:** No raise_for_status. Auth errors or rate limit produce cryptic JSONDecodeError or KeyError on tokens.
- **Suggested fix:** Add resp.raise_for_status() before json.loads.
- **Confidence:** high

### [HIGH-17] Module-level side-effects run at import time ⭐
- **File:** `scripts/create_c4_sequences.py:48` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
  access_token = get_access_token()
  HEADERS = {
      "Authorization": f"Bearer {access_token}",
      "Content-Type": "application/vnd.api+json",
  }
  ```
- **Why it matters:** GitHub fetch and OAuth refresh fire on import, not main(). Any failure raises before __main__ check.
- **Suggested fix:** Move inside main() or lazy helper.
- **Confidence:** high

### `scripts/delete_zzz_sentinels.py` (post-S40 commits: 1)

### [HIGH-19] batchUpdate response not checked; silent failure on partial delete ⭐
- **File:** `scripts/delete_zzz_sentinels.py:68` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
  svc.spreadsheets().batchUpdate(
      spreadsheetId=sheet_id, body={'requests': requests}
  ).execute()
  print(f'Deleted {len(zzz_rows)} sentinel rows.')
  ```
- **Why it matters:** No try/except. Partial failures leave sheet in inconsistent state with misleading success print.
- **Suggested fix:** Wrap in try/except HttpError with non-zero exit.
- **Confidence:** high

### `scripts/enrich_c4_pass2.py` (post-S40 commits: 1)

### [HIGH-22] Title and INTL notes writes for same row clobber each other ⭐
- **File:** `scripts/enrich_c4_pass2.py:987` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
          # Title update → append to Notes
          if "title" in upd:
              # Read current notes
              current_row = pq_rows[row_idx - 1] if row_idx - 1 < len(pq_rows) else []
              current_notes = current_row[notes_col] if len(current_row) > notes_col else ""
  
              # Check if already has a title
              existing_title = extract_title_from_notes(current_notes)
  ```
- **Why it matters:** Two separate sheet_updates entries for same Notes cell. RAW write applies in order; second overwrites first. Both pieces of data can't coexist.
- **Suggested fix:** Merge all fragments into single Notes string before appending to sheet_updates.
- **Confidence:** high

### [HIGH-23] INTL notes append produces leading ' | ' when current_notes is empty ⭐
- **File:** `scripts/enrich_c4_pass2.py:1019` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
              new_notes = current_notes + f" | INTL: {upd['international']}"
  ```
- **Why it matters:** Empty current_notes + append produces leading ' | INTL: ca'. Corrupts format and breaks extract_title_from_notes parsing.
- **Suggested fix:** Conditional: INTL first if current_notes empty.
- **Confidence:** high

### `scripts/enrich_c4_titles.py` (post-S40 commits: 1)

### [HIGH-24] Layer 1 sequence scan loop is dead code ⭐
- **File:** `scripts/enrich_c4_titles.py:384` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
      for seq_id in sequence_ids:
          logger.info(f"  Scanning sequence {seq_id}...")
          try:
              states = outreach_get_all(
                  "/sequenceStates",
                  {"filter[sequence][id]": str(seq_id), "include": "prospect"}
              )
          except Exception as e:
  ```
- **Why it matters:** First loop's states variable never used; inner body is pass. Second loop re-scans same IDs doubling API calls.
- **Suggested fix:** Remove the first loop entirely.
- **Confidence:** high

### [HIGH-25] Notes column index hardcoded as S without verification ⭐
- **File:** `scripts/enrich_c4_titles.py:768` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
                  # Notes is column S (index 19 = column 19 = S)
                  updates.append({
                      "range": f"'Prospecting Queue'!S{i}",
                      "values": [[new_notes]],
                  })
  ```
- **Why it matters:** If sheet is reordered, writes go to wrong column silently.
- **Suggested fix:** Look up notes_col from headers dynamically.
- **Confidence:** high

### `scripts/f4_serper_replay.py` (post-S40 commits: 1)

### [HIGH-26] .env read crashes if file missing with no helpful error ⭐
- **File:** `scripts/f4_serper_replay.py:41` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `54e7fed` (no session tag)
- **Evidence:**
  ```python
  for ln in (ROOT / ".env").read_text().splitlines():
  ```
- **Why it matters:** FileNotFoundError raw traceback before SERPER_API_KEY check can emit useful message.
- **Suggested fix:** Wrap in try/except FileNotFoundError or check exists() first.
- **Confidence:** high

### `scripts/phase1b_adjacent_rows.py` (post-S40 commits: 1)

### [HIGH-30] KeyError on missing env vars crashes at module level ⭐
- **File:** `scripts/phase1b_adjacent_rows.py:35` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  creds = Credentials.from_service_account_info(
      json.loads(os.environ["GOOGLE_SERVICE_ACCOUNT_JSON"]),
      scopes=["https://www.googleapis.com/auth/spreadsheets.readonly"],
  )
  svc = build("sheets", "v4", credentials=creds)
  sheet_id = os.environ["GOOGLE_SHEETS_ID"]
  ```
- **Why it matters:** Raw KeyError with no diagnostic message on missing credentials.
- **Suggested fix:** Use explicit env var check with sys.exit message.
- **Confidence:** medium

### `scripts/phase1d_local_sentinel.py` (post-S40 commits: 1)

### [HIGH-31] Raw .env parsing crashes on missing file ⭐
- **File:** `scripts/phase1d_local_sentinel.py:19` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  for line in env_path.read_text().splitlines():
      if "=" in line and not line.startswith("#"):
          k, v = line.split("=", 1)
          os.environ.setdefault(k.strip(), v.strip())
  ```
- **Why it matters:** FileNotFoundError with no helpful message. Quoted values also not unquoted.
- **Suggested fix:** Wrap in try/except; strip quotes.
- **Confidence:** medium

### [HIGH-32] KeyError on missing credentials occurs AFTER sentinel already written ⭐
- **File:** `scripts/phase1d_local_sentinel.py:47` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  creds = Credentials.from_service_account_info(
      json.loads(os.environ["GOOGLE_SERVICE_ACCOUNT_JSON"]),
      scopes=["https://www.googleapis.com/auth/spreadsheets.readonly"],
  )
  svc = build("sheets", "v4", credentials=creds)
  sheet_id = os.environ["GOOGLE_SHEETS_ID"]
  ```
- **Why it matters:** Script writes to sheet first then fails on read-back, leaving orphaned sentinel row with no cleanup.
- **Suggested fix:** Check env vars immediately after load_dotenv, before dp.add_district.
- **Confidence:** high

### `scripts/phase1e_executor_test.py` (post-S40 commits: 1)

### [HIGH-33] Sentinel row written to production sheet, never cleaned up ⭐
- **File:** `scripts/phase1e_executor_test.py:24` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
      result = await loop.run_in_executor(
          None,
          lambda: dp.add_district(
              name=SENTINEL_NAME,
              state="TX",
              notes="Session 54 Phase 1e sentinel — via executor",
              strategy="competitor_displacement",
              source="signal",
              signal_id="sentinel_phase1e_executor",
              est_enrollment=9999,
          )
      )
  ```
- **Why it matters:** Each run permanently pollutes production Prospecting Queue with fake entry.
- **Suggested fix:** Add delete after verification or use a test spreadsheet ID.
- **Confidence:** high

### `scripts/phase1g_railway_env_sentinel.py` (post-S40 commits: 1)

### [HIGH-34] No error handling around add_district write before Sheets read ⭐
- **File:** `scripts/phase1g_railway_env_sentinel.py:16` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  r = dp.add_district(
      name=SENTINEL,
      state="TX",
      notes="Phase 1g — railway env test",
      strategy="competitor_displacement",
      source="signal",
      signal_id=f"phase1g_{random.randint(1000,9999)}",
      est_enrollment=5555,
  )
  ```
- **Why it matters:** If add_district fails, script prints 'sentinel not found' masking the real failure.
- **Suggested fix:** Check return value and exit with meaningful error.
- **Confidence:** high

### `scripts/phase2_fingerprint_audit.py` (post-S40 commits: 1)

### [HIGH-35] IndexError crash when sheet returns no rows ⭐
- **File:** `scripts/phase2_fingerprint_audit.py:72` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  header = rows[0]
  data_rows = rows[1:]
  ```
- **Why it matters:** Empty values list raises IndexError. .get('values', []) guards missing key but not empty list.
- **Suggested fix:** Add if not rows: sys.exit('Sheet returned no data').
- **Confidence:** high

### `scripts/phase2b_sample_fingerprints.py` (post-S40 commits: 1)

### [HIGH-36] env_path.read_text() raises FileNotFoundError with no user-friendly error ⭐
- **File:** `scripts/phase2b_sample_fingerprints.py:9` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  for line in env_path.read_text().splitlines():
  ```
- **Why it matters:** Cryptic traceback if .env missing.
- **Suggested fix:** Guard with exists() and print clear message.
- **Confidence:** medium

### `scripts/repair_scrambled_queue_rows.py` (post-S40 commits: 1)

### [HIGH-37] _base_fields reads date_approved/sequence_doc_url from unverified scrambled indices 15/16 ⭐
- **File:** `scripts/repair_scrambled_queue_rows.py:115` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
          "date_approved": padded[15] if len(padded) > 15 else "",
          "sequence_doc_url": padded[16] if len(padded) > 16 else "",
  ```
- **Why it matters:** Scramble layout comment doesn't document positions 15/16. Silently reads whatever lives there, potentially corrupting data for never-approved rows.
- **Suggested fix:** Verify positions empirically or default to empty string.
- **Confidence:** medium

### [HIGH-38] Unknown rows silently preserved and written back verbatim on --apply ⭐
- **File:** `scripts/repair_scrambled_queue_rows.py:344` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
          else:
              # Keep unknown row as-is, padded to 20
              padded = row + [""] * max(0, NUM_COLS - len(row))
              new_rows.append([str(v) if v is not None else "" for v in padded[:NUM_COLS]])
  ```
- **Why it matters:** Unclassified rows included in destructive clear+rewrite without warning. New strategy variants silently committed.
- **Suggested fix:** Skip unknown rows or abort --apply unless --allow-unknown flag set.
- **Confidence:** high

### `scripts/s60_schedule_lookup.py` (post-S40 commits: 1)

### [HIGH-43] _api_get('/sequences/1999') missing include=schedule param ⭐
- **File:** `scripts/s60_schedule_lookup.py:71` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `846aaed` (no session tag)
- **Evidence:**
  ```python
          sched_id = sched.get("id") if sched else None
  ```
- **Why it matters:** Section header promises include=schedule but call to _api_get omits params. Sideload block always produces empty output.
- **Suggested fix:** Pass {'include': 'schedule'} as params to _api_get.
- **Confidence:** high

### `scripts/s60_test_target_lookup.py` (post-S40 commits: 1)

### [HIGH-44] Unguarded JSON parse on missing env vars ⭐
- **File:** `scripts/s60_test_target_lookup.py:99` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** Session 60 (`616e184`)
- **Evidence:**
  ```python
  creds_json = os.environ.get("GOOGLE_SERVICE_ACCOUNT_JSON")
  creds_dict = _json.loads(creds_json)
  ```
- **Why it matters:** os.environ.get returns None, _json.loads(None) raises TypeError with cryptic message.
- **Suggested fix:** Use os.environ[] for clear KeyError or explicit if-none guard.
- **Confidence:** high

### [HIGH-45] em_sorted indexed using n from yields, not with_email_yields length ⭐
- **File:** `scripts/s60_test_target_lookup.py:153` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** Session 60 (`616e184`)
- **Evidence:**
  ```python
      em_sorted = sorted(with_email_yields)
      print(f"\nContacts-with-email distribution:")
      print(f"  median: {em_sorted[n // 2]}")
      print(f"  mean: {sum(with_email_yields) / n:.1f}")
      print(f"  zero-email jobs: {sum(1 for y in with_email_yields if y == 0)} ({100 * sum(1 for y in with_email_yields if y == 0) / n:.0f}%)")
  ```
- **Why it matters:** n is len(yields); if lists diverge, wrong statistic or IndexError. Not self-documenting.
- **Suggested fix:** Recompute em_n separately.
- **Confidence:** medium

### `scripts/telethon_auth.py` (post-S40 commits: 1)

### [HIGH-46] Hardcoded phone number ⭐
- **File:** `scripts/telethon_auth.py:16` @ `339c92c`
- **Category:** Security
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
  PHONE = "+14058355067"
  ```
- **Why it matters:** Real phone number hardcoded in source and committed to repo. PII and Telegram account identity exposed in version control history.
- **Suggested fix:** Read from environment variable os.environ['TELEGRAM_PHONE'].
- **Confidence:** high

### `scripts/test_load_campaign.py` (post-S40 commits: 1)

### [HIGH-47] _read_contacts_csv called with raw CSV string instead of file path ⭐
- **File:** `scripts/test_load_campaign.py:98` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `6ea3b29` (no session tag)
- **Evidence:**
  ```python
          load_campaign._read_contacts_csv(bad)
  ```
- **Why it matters:** If function expects file path, raw CSV string causes file-not-found rather than expected ValueError. Test produces false result.
- **Suggested fix:** Write CSV to temp file and pass path.
- **Confidence:** medium

### [HIGH-48] Second raw-CSV call with extra_columns ⭐
- **File:** `scripts/test_load_campaign.py:106` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `6ea3b29` (no session tag)
- **Evidence:**
  ```python
      rows = load_campaign._read_contacts_csv(
          "first_name,last_name,email,title,company,state,notes,extra\n"
          "A,B,a@b.com,Principal,ISD,TX,,junk\n"
      )
  ```
- **Why it matters:** Same interface assumption issue as previous test.
- **Suggested fix:** Use temp file pattern.
- **Confidence:** medium

### `scripts/tg_recent.py` (post-S40 commits: 1)

### [HIGH-51] No error handling around connect/iter_messages; client left connected on exception ⭐
- **File:** `scripts/tg_recent.py:14` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
      c = TelegramClient(SESSION, API_ID, API_HASH)
      await c.connect()
      async for msg in c.iter_messages(target, limit=n):
          ts = msg.date.isoformat()
          who = "BOT" if msg.out is False else "ME "
          text = (msg.text or msg.message or "").replace("\n", " | ")[:500]
          print(f"{ts}  {who}  id={msg.id}  {text}")
      await c.disconnect()
  ```
- **Why it matters:** If `c.iter_messages()` or any message processing raises an exception, `c.disconnect()` is never called. This leaks the Telethon session lock file and can prevent subsequent runs from connecting.
- **Suggested fix:** Wrap the body of `main` in a try/finally block so `await c.disconnect()` is guaranteed to run, or use `async with TelegramClient(...) as c:` which handles connect/disconnect automatically.
- **Confidence:** high

### [HIGH-52] BOT/ME label logic is inverted ⭐
- **File:** `scripts/tg_recent.py:18` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
          who = "BOT" if msg.out is False else "ME "
  ```
- **Why it matters:** In Telethon, `msg.out` is True when the authenticated account sent the message, and False (or None) when it came from the remote party. The condition `msg.out is False` therefore labels incoming messages as 'BOT' and outgoing messages as 'ME', which is backwards for the intent implied by the label names.
- **Suggested fix:** Change the condition to `msg.out is True` (or just `msg.out`) to label outgoing messages as 'ME' and incoming as 'BOT'. Also consider handling `None` explicitly since `msg.out` can be None for channel posts.
- **Confidence:** high

### `scripts/tg_send.py` (post-S40 commits: 1)

### [HIGH-53] Client left connected on send failure ⭐
- **File:** `scripts/tg_send.py:21` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
      sent = await c.send_message(target, message)
      print(f"Sent id={sent.id} to={target} at {sent.date.isoformat()}")
      await c.disconnect()
  ```
- **Why it matters:** No try/finally. Session file locked on exception.
- **Suggested fix:** Wrap in try/finally or use async with.
- **Confidence:** high

### [HIGH-54] No argv bounds check — IndexError on missing args ⭐
- **File:** `scripts/tg_send.py:26` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
      asyncio.run(main(sys.argv[1], sys.argv[2]))
  ```
- **Why it matters:** Missing args raise unhandled IndexError.
- **Suggested fix:** Check len(sys.argv) < 3 and print usage.
- **Confidence:** high

### `tools/daily_call_list.py` (post-S40 commits: 1)

### [HIGH-59] Backfill district key uses raw lead field, not display_name — _MAX_PER_DISTRICT bypass ⭐
- **File:** `tools/daily_call_list.py:277` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `ee1ca2c` (no session tag)
- **Evidence:**
  ```python
              lead_district = (lead.get("District Name") or lead.get("Account") or "").strip()
              if lead_district:
                  if district_counts.get(lead_district, 0) >= _MAX_PER_DISTRICT:
                      continue
                  district_counts[lead_district] = district_counts.get(lead_district, 0) + 1
  ```
- **Why it matters:** Main loop stores keys via display_name; backfill uses raw fields. Different key strings mean a district can have 2 contacts from main pass and 2 more from backfill pass, allowing 4 contacts per district.
- **Suggested fix:** Normalize backfill district key same way as main loop.
- **Confidence:** high

### `tools/prospect_loader.py` (post-S40 commits: 1)

### [HIGH-65] Sequence-enabled cache never invalidated mid-run ⭐
- **File:** `tools/prospect_loader.py:314` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
          # Cache sequence-enabled status per sequence to avoid repeat reads
          seq_enabled_cache: dict[str, bool] = {}
  ```
- **Why it matters:** If sequence is paused after first check (concurrent user action), all remaining contacts get added to paused sequence. Exact failure mode preflight check was designed to prevent.
- **Suggested fix:** Remove cache or invalidate periodically. 5-15 min sleep cadence means cache provides minimal benefit.
- **Confidence:** high

### [HIGH-66] create_prospect called with hardcoded owner_id=11 bypassing LoadPlan config ⭐
- **File:** `tools/prospect_loader.py:353` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
              result = create_prospect(
                  first_name=contact.first_name,
                  last_name=contact.last_name,
                  email=email_clean,
                  title=contact.title,
                  company=contact.company,
                  state=contact.state,
                  timezone=state_to_timezone(contact.state) or "",
                  tags=plan.tags,
                  owner_id=11,
              )
  ```
- **Why it matters:** Hardcoded integer owner. If workspace owner changes, all prospects assigned to wrong owner silently. plan.mailbox_id threaded through LoadPlan but owner_id bypasses it.
- **Suggested fix:** Accept optional owner_id parameter in execute_load_plan and pass through.
- **Confidence:** medium


## Medium (114)

### `agent/main.py`

### [MED-3] `asyncio.get_event_loop()` used throughout async handlers — deprecated pattern in Python 3.10+ ⭐
- **File:** `agent/main.py:1007` @ `339c92c`
- **Category:** Concurrency
- **Introduced in:** `a2ef953` (no session tag)
- **Evidence:**
  ```python
              loop = asyncio.get_event_loop()
              result = await loop.run_in_executor(None, activity_tracker.sync_gmail_activities, gas)
  ```
- **Why it matters:** `asyncio.get_event_loop()` is deprecated inside coroutines since Python 3.10 and raises a `DeprecationWarning` (and in some 3.12 builds, an error). The correct idiom inside an `async def` is `asyncio.get_running_loop()`. The pattern appears across ~30+ call sites throughout the file.
- **Suggested fix:** Replace all `asyncio.get_event_loop()` calls inside async functions with `asyncio.get_running_loop()`.
- **Confidence:** high

### [MED-5] Morning brief `send_morning_brief` opens prompt file with a relative path that breaks on Railway ⭐
- **File:** `agent/main.py:3982` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `b39c4ce` (no session tag)
- **Evidence:**
  ```python
          with open("prompts/morning_brief.md", "r") as f:
              prompt_template = f.read()
  ```
- **Why it matters:** Both `send_morning_brief` (line 3982) and `send_eod_report` (line 4185) open files using bare relative paths. The working directory on Railway may not be the repo root, causing silent `FileNotFoundError` failures that are caught by the outer `except` and simply log the error without alerting the user.
- **Suggested fix:** Use `pathlib.Path(__file__).resolve().parent.parent / "prompts" / "morning_brief.md"` to make paths absolute and repo-root-relative.
- **Confidence:** medium

### `tools/signal_processor.py`

### [MED-111] compute_heat_score accepts days_old parameter but never uses it ⭐
- **File:** `tools/signal_processor.py:618` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `7c2b7f8` (no session tag)
- **Evidence:**
  ```python
  def compute_heat_score(signal_type: str, tier: int, in_territory: bool,
                         customer_status: str, cluster_count: int = 1,
                         days_old: int = 0) -> int:
  ```
- **Why it matters:** `days_old` is in the function signature (with comment "Decay is applied separately on read") but is never referenced in the function body. Future callers reading the signature may incorrectly believe recency is factored into the base score.
- **Suggested fix:** Remove `days_old` from the signature to eliminate the misleading dead parameter, or document explicitly in the docstring that it is intentionally unused.
- **Confidence:** high

### [MED-112] NCES state correction applied after territory filter — corrected state may be non-territory ⭐
- **File:** `tools/signal_processor.py:2632` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `72c9b71` (no session tag)
- **Evidence:**
  ```python
          # Validate state is in territory
          if state not in TERRITORY_STATES_WITH_CA:
              continue
  
          # NCES lookup for validation/correction
          nces_state = lookup_district_state(district)
          if nces_state:
              state = nces_state
  ```
- **Why it matters:** In `scan_rfp_opportunities` (and identically in other scan functions), the territory filter passes based on Claude's stated state, then NCES overrides the state. If NCES corrects Claude's territory state to a non-territory state, the signal is emitted with the corrected (non-territory) state — bypassing the filter entirely.
- **Suggested fix:** Apply NCES correction before the territory filter, or add a second territory check after the NCES correction to drop newly-out-of-territory signals.
- **Confidence:** medium

### [MED-113] Non-standard signal_type values written to Signals tab break display and downstream logic ⭐
- **File:** `tools/signal_processor.py:3577` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `6852292` (no session tag)
- **Evidence:**
  ```python
                  "signal_type": "cs_funding_award",
  ```
- **Why it matters:** The values `cs_funding_award` (lines 3577, 3619) and `competitor_usage` (lines 3949, 4005) are not in SIGNAL_WEIGHTS, SIGNAL_PATTERNS, _SIGNAL_TYPE_TAGS, or the documented signal_type enum. Signals with these types display as generic "SIG" in Telegram, get base weight 5 instead of the grant/market_intel weight, and cannot be correctly processed by downstream code.
- **Suggested fix:** Map these to existing canonical types: `cs_funding_award` → `"grant"` and `competitor_usage` → `"market_intel"`. If a separate type is needed, add entries to SIGNAL_WEIGHTS and _SIGNAL_TYPE_TAGS.
- **Confidence:** high

### `tools/district_prospector.py`

### [MED-92] clear_by_strategy wrong fallback index for strategy_idx — uses 5 (Deal Level) instead of 8 (Strategy) ⭐
- **File:** `tools/district_prospector.py:314` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5a16c61` (no session tag)
- **Evidence:**
  ```python
      strategy_idx = headers.index("Strategy") if "Strategy" in headers else 5
  ```
- **Why it matters:** In the 20-column schema, Strategy is at zero-based index 8, not 5. Index 5 is 'Deal Level'. If the Prospecting Queue header row is ever missing the 'Strategy' column, the fallback causes `clear_by_strategy` to filter on Deal Level values instead of strategy values. The analogous fallback in `cleanup_prospect_queue` (line 374) correctly uses `else 8`.
- **Suggested fix:** Change the fallback from `else 5` to `else 8` to match the correct 0-based column index for Strategy.
- **Confidence:** high

### [MED-93] suggest_sequence_reengagement documented in CLAUDE.md API table but does not exist in source ⭐
- **File:** `tools/district_prospector.py:3224` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `0838466` (no session tag)
- **Evidence:**
  ```python
  _C4_SEQUENCE_IDS = {1995, 1996, 1997, 1998}
  
  
  def get_sequence_reengagement_overview(exclude_sequence_ids=None) -> dict:
  ```
- **Why it matters:** CLAUDE.md lists `district_prospector.suggest_sequence_reengagement(...)` as a public function. The function does not exist — it was split into `get_sequence_reengagement_overview` and `scan_sequence_for_reengagement` during a refactor, but CLAUDE.md was not updated.
- **Suggested fix:** Update CLAUDE.md to replace the `suggest_sequence_reengagement` entry with the two replacement functions.
- **Confidence:** high

### `tools/outreach_client.py`

### [MED-96] _api_patch accepts HTTP 201 as success; will raise on valid HTTP 204 ⭐
- **File:** `tools/outreach_client.py:384` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `ffadef5` (no session tag)
- **Evidence:**
  ```python
      if resp.status_code not in (200, 201):
          body = resp.text[:500]
          logger.error(f"Outreach PATCH {path} HTTP {resp.status_code}: {body}")
          raise Exception(f"Outreach API error {resp.status_code}: {body}")
  ```
- **Why it matters:** HTTP 201 Created is semantically wrong for a PATCH response. More critically, HTTP 204 No Content is a valid and common PATCH success response per RFC 9110; if the Outreach API ever returns 204, this function raises an exception and callers lose the update silently.
- **Suggested fix:** Change the allowlist to `(200, 204)`. Add a guard so the function returns `{}` when the status is 204 rather than calling `resp.json()` on an empty body.
- **Confidence:** medium

### [MED-97] verify_sequence issues 2N serial API calls to fetch step templates ⭐
- **File:** `tools/outreach_client.py:1113` @ `339c92c`
- **Category:** Performance red flags
- **Introduced in:** `1f22991` (no session tag)
- **Evidence:**
  ```python
      for i, step in enumerate(raw_steps):
          step_id = step.get("id")
          st_resp = _try_fetch("/sequenceTemplates", params={"filter[sequenceStep][id]": str(step_id)})
          if st_resp is None:
              warnings.append(f"could not fetch sequenceTemplates for step {step_id}; skipping body scan")
              continue
  ```
- **Why it matters:** For each of N steps, the loop makes two sequential API calls totalling 2N blocking HTTP requests. For a 7-step sequence this is 14+ serial calls; verify_sequence is called automatically after every create_sequence invocation.
- **Suggested fix:** Batch the template body fetches in parallel using httpx, or cache sequenceTemplate results from the export_sequence code path to avoid duplicate fetches.
- **Confidence:** high

### `tools/research_engine.py`

### [MED-104] `district_hint` in `_layer10_dedup_and_score` is computed but never used ⭐
- **File:** `tools/research_engine.py:893` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `0ea7cfe` (no session tag)
- **Evidence:**
  ```python
          district_hint = self.district_name.lower()
          for suffix in [" isd", " unified school district", " school district",
                         " public schools", " city schools",
                         " county superintendent of schools", " county schools"]:
              if district_hint.endswith(suffix):
                  district_hint = district_hint[:-len(suffix)]
                  break
          district_hint = district_hint.replace(" ", "")
  ```
- **Why it matters:** Lines 893-900 derive a normalized `district_hint` but this variable is never referenced again. The actual cross-district filtering uses `_district_name_hint()`. Dead code block left over from a refactor.
- **Suggested fix:** Remove the dead `district_hint` computation block.
- **Confidence:** high

### [MED-105] L18 Firecrawl contacts assigned `VERIFIED` confidence without cross-district filtering ⭐
- **File:** `tools/research_engine.py:1232` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `4ce8656` (no session tag)
- **Evidence:**
  ```python
                          "email_confidence": "VERIFIED" if email and "***" not in email else "UNKNOWN",
  ```
- **Why it matters:** L18 (Firecrawl extract) sets `email_confidence = 'VERIFIED'` for any non-masked email. This bypasses both the Stage 1 page filter and the Stage 2 contact filter. Firecrawl's AI extraction can return contacts from cross-district pages, and those contacts enter `all_contacts` pre-tagged VERIFIED.
- **Suggested fix:** Use `'LIKELY'` as the maximum for Firecrawl-sourced contacts, and let L10's `known_emails` check be the only path to VERIFIED.
- **Confidence:** medium

### [MED-106] `_discover_links` calls `crawled.discard()` instead of `crawled.add()`, defeating dedup ⭐
- **File:** `tools/research_engine.py:1675` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `dbcadc2` (no session tag)
- **Evidence:**
  ```python
                  crawled.discard(full)  # allow recrawl discovery — we'll dedupe elsewhere
  ```
- **Why it matters:** `_discover_links` is called from `_layer6_direct_scrape` to populate the `crawled` set with discovered links so they are not re-fetched in the seed loop. Using `discard` removes any URL that was already marked as crawled rather than adding the newly discovered one.
- **Suggested fix:** Change `crawled.discard(full)` to `crawled.add(full)`.
- **Confidence:** high

### `agent/scheduler.py`

### [MED-6] mark_user_active_today has no effect: weekend path exits before greeting suppression is checked ⭐
- **File:** `agent/scheduler.py:23` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `8e1c305` (no session tag)
- **Evidence:**
  ```python
      def mark_user_active_today(self):
          """Called from handle_message on weekends to suppress auto-greeting."""
          today = datetime.now(CST).date()
          self._user_active_today = True
          self._user_active_date = today
  ```
- **Why it matters:** check() returns None immediately for weekday >= 5 before _is_user_active_today() is ever consulted, so the suppression logic can never take effect even if weekend_greeting is re-enabled.
- **Suggested fix:** Move the _is_user_active_today() guard inside the weekend branch.
- **Confidence:** high

### [MED-7] weekend_greeting and checkin events documented in CLAUDE.md but never emitted ⭐
- **File:** `agent/scheduler.py:44` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `5754efa` (no session tag)
- **Evidence:**
  ```python
          # ── Weekend: no scheduled events ──
          if weekday >= 5:
              return None
  ```
- **Why it matters:** CLAUDE.md documents that check() can return 'weekend_greeting' (Sat 11am, Sun 1pm) and 'checkin', but the weekend branch unconditionally returns None and there is no checkin block anywhere in check().
- **Suggested fix:** Either re-implement the weekend_greeting and checkin blocks, or remove those return values from the CLAUDE.md contract.
- **Confidence:** high

### [MED-8] Minute-exact schedule requires sub-minute polling cadence or events are silently dropped ⭐
- **File:** `agent/scheduler.py:48` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `7c2b7f8` (no session tag)
- **Evidence:**
  ```python
          if hour == 7 and minute == 45:
              if self._last_signal_scan_date != today:
                  self._last_signal_scan_date = today
                  return "signal_scan"
  ```
- **Why it matters:** Every scheduled trigger fires only when the exact minute matches. If check() is polled less frequently than once per minute, the matching minute can be skipped entirely.
- **Suggested fix:** Switch to a 'greater-than-or-equal and not-yet-fired-today' pattern.
- **Confidence:** medium

### `tools/email_drafter.py`

### [MED-94] draft_for_sender never marks message_id as processed — background cycle will re-attempt the same draft ⭐
- **File:** `tools/email_drafter.py:415` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `37acfeb` (no session tag)
- **Evidence:**
  ```python
      from_addr = match.get("from", "")
      sender_email = _extract_sender_email(from_addr)
      sender_name = _extract_sender_name(from_addr)
      subject = match.get("subject", "")
      thread_id = match.get("thread_id", "")
  ```
- **Why it matters:** After a targeted `/draft [name]` call, the message_id is never added to `_processed_message_ids`. The background `process_new_emails` cycle will encounter the same unread thread on its next run, classify it via Haiku, draft via Sonnet, then hit the `already_drafted` path — wasting two API calls per cycle.
- **Suggested fix:** After a successful `gas.create_draft()` call in `draft_for_sender`, add `match.get('message_id', '')` to `_processed_message_ids`.
- **Confidence:** high

### `tools/private_schools.py`

### [MED-98] Serper HTTP error silently produces empty results — no status check before resp.json() ⭐
- **File:** `tools/private_schools.py:170` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `c911b33` (no session tag)
- **Evidence:**
  ```python
              data = resp.json()
              for item in data.get("organic", [])[:10]:
  ```
- **Why it matters:** resp.json() is called unconditionally — a 429 rate-limit or 401 auth-error response from Serper returns non-JSON or an error body with no 'organic' key, so the loop silently yields zero results with no log entry indicating the actual HTTP failure.
- **Suggested fix:** Call resp.raise_for_status() before calling resp.json(). Log the status code alongside the warning.
- **Confidence:** high

### `gas/Code.gs`

### [MED-10] threadHasDraft fetches all drafts on every createDraft reply call ⭐
- **File:** `gas/Code.gs:219` @ `339c92c`
- **Category:** Performance
- **Introduced in:** `97128c9` (no session tag)
- **Evidence:**
  ```python
      var drafts = GmailApp.getDrafts();
      for (var i = 0; i < drafts.length; i++) {
        try {
          var msg = drafts[i].getMessage();
          if (msg && msg.getThread().getId() === threadId) {
  ```
- **Why it matters:** O(n) scan with n API calls inside the loop. Hits GAS execution-time limits for large mailboxes.
- **Suggested fix:** Use GmailApp.search or cache draft list.
- **Confidence:** medium

### [MED-11] searchInbox returns first message of thread instead of most-recent ⭐
- **File:** `gas/Code.gs:272` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `863facc` (no session tag)
- **Evidence:**
  ```python
      var msg = threads[i].getMessages()[0];
  ```
- **Why it matters:** getSentEmails uses messages[length-1] (most-recent) but searchInbox uses [0] (oldest). Inbox callers likely want latest reply.
- **Suggested fix:** Use last element, consistent with getSentEmails.
- **Confidence:** medium

### [MED-12] createCalendarEvent does not validate start_iso / end_iso ⭐
- **File:** `gas/Code.gs:434` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `863facc` (no session tag)
- **Evidence:**
  ```python
    var start = new Date(params.start_iso);
    var end = new Date(params.end_iso);
  ```
- **Why it matters:** NaN dates silently create broken events. Opaque error or epoch-zero event.
- **Suggested fix:** Check isNaN(start.getTime()) before calling createEvent.
- **Confidence:** high

### `tools/gas_bridge.py`

### [MED-95] get_sent_emails docstring promises has_more but method returns list, dropping it ⭐
- **File:** `tools/gas_bridge.py:99` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `6add3d8` (no session tag)
- **Evidence:**
  ```python
          Caller checks data["has_more"] to decide whether to fetch the next page.
          """
          data = self._call("get_sent_emails", {
              "months_back": months_back,
              "page_size":   page_size,
              "page_start":  page_start,
          })
          emails = data.get("emails", [])
  ```
- **Why it matters:** Docstring tells callers to check data['has_more'] but method returns only emails list. Callers following the docstring will get AttributeError or never paginate correctly.
- **Suggested fix:** Update docstring or change return type to include has_more in a dict.
- **Confidence:** high

### `scripts/rule_scanner.py`

### [MED-59] scan() returns only first triggered rule ID even when multiple rules fire ⭐
- **File:** `scripts/rule_scanner.py:300` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `94cdc0e` (no session tag)
- **Evidence:**
  ```python
              if triggered_rule_id is None:
                  triggered_rule_id = rule["id"]
  ```
- **Why it matters:** When R19 and R20 both fire, triggered_rule_id is set to only whichever appears first in RULES. Returned dict's 'rule' field silently drops the second rule's ID.
- **Suggested fix:** Return a list of all triggered rule IDs.
- **Confidence:** medium

### `tools/proximity_engine.py`

### [MED-102] CLAUDE.md documents a stale/nonexistent proximity_engine public API ⭐
- **File:** `tools/proximity_engine.py:190` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `4e5d6c5` (no session tag)
- **Evidence:**
  ```python
  def find_nearby_one(account_name: str, radius_miles: float = 15,
                      min_enrollment: int = 0) -> dict:
  ```
- **Why it matters:** tools/CLAUDE.md lists find_nearby_districts, format_proximity_for_telegram, and a different add_proximity_prospects signature — none exist. Actual API is find_nearby_one, find_nearby_state, format_targeted_for_telegram, format_state_sweep_for_telegram.
- **Suggested fix:** Update tools/CLAUDE.md proximity_engine section.
- **Confidence:** high

### [MED-103] format_targeted_for_telegram school groups not sorted intra-group by distance ⭐
- **File:** `tools/proximity_engine.py:743` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `4e5d6c5` (no session tag)
- **Evidence:**
  ```python
          for dn, school_list in sorted(by_district.items(), key=lambda x: x[1][0]["distance_miles"]):
  ```
- **Why it matters:** Sort assumes school_list[0] is always closest, but grouping by district may leave intra-group lists unsorted if haversine ordering wasn't preserved.
- **Suggested fix:** Sort each school_list by distance_miles before reading school_list[0].
- **Confidence:** medium

### `tools/territory_map.py`

### [MED-115] pipeline_tracker state filter uses substring match on Account Name ⭐
- **File:** `tools/territory_map.py:47` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `05f2d80` (no session tag)
- **Evidence:**
  ```python
              sf = state_filter.strip().upper()
              pipeline_opps = [o for o in pipeline_opps if sf in o.get("Account Name", "").upper()
                              or o.get("State", "").upper() == sf]
  ```
- **Why it matters:** Substring match on Account Name produces false positives — state_filter='OK' matches any account name containing 'OK'.
- **Suggested fix:** Filter exclusively on the State field.
- **Confidence:** high

### [MED-116] Bare except swallows all errors from pipeline_tracker and district_prospector ⭐
- **File:** `tools/territory_map.py:50` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `05f2d80` (no session tag)
- **Evidence:**
  ```python
      except Exception:
          pipeline_opps = []
      logger.info(f"Pipeline opps: {len(pipeline_opps)}")
  ```
- **Why it matters:** No log of the error. Misconfiguration or Sheets outage silently produces empty map layer.
- **Suggested fix:** Log exception at WARNING with exc_info=True.
- **Confidence:** high

### `scripts/s59_regen_diocesan_sequences.py`

### [MED-60] build_sequence failure and Outreach push failure indistinguishable in summary ⭐
- **File:** `scripts/s59_regen_diocesan_sequences.py:191` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `7c162b6` (no session tag)
- **Evidence:**
  ```python
      if not seq_result.get("success"):
          print(f'  BUILD FAILED: {seq_result.get("error", "?")}')
          results[district_name] = {"error": "build_failed"}
          continue
  ```
- **Why it matters:** Summary loop prints 'FAIL' for both build failures and Outreach push failures.
- **Suggested fix:** Add distinct status key (build_failed vs outreach_failed).
- **Confidence:** medium

### [MED-61] step1_words counted before HTML conversion — count inflated by anchor tags ⭐
- **File:** `scripts/s59_regen_diocesan_sequences.py:200` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** Session 59 (`4051f53`)
- **Evidence:**
  ```python
      step1_words = len(steps[0]['body'].split()) if steps else 0
  ```
- **Why it matters:** Word count includes raw anchor tag fragments, producing misleading number that doesn't reflect rendered word count.
- **Suggested fix:** Strip HTML tags before splitting on whitespace.
- **Confidence:** medium

### `scripts/test_round1_unit.py`

### [MED-74] monkeypatch not restored if test body raises before undo ⭐
- **File:** `scripts/test_round1_unit.py:398` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `978499b` (no session tag)
- **Evidence:**
  ```python
      monkeypatch(ce, "extract_contacts", fake_extract_contacts)
  
      pages = [("http://a/", "x"), ("http://b/", "y")]
      result = ce.extract_from_multiple(pages, "Test ISD")
  
      assert len(result) == 1, f"expected 1 merged contact, got {len(result)}: {result}"
      assert result[0]["email"] == "john.smith@test.edu"
      assert result[0]["email_confidence"] == "VERIFIED"
  ```
- **Why it matters:** If ce.extract_from_multiple raises unexpectedly, patched ce.extract_contacts remains installed until outer cleanup.
- **Suggested fix:** Wrap post-patch section in try/finally inside the test.
- **Confidence:** low

### [MED-75] test_extract_from_multiple_upgrades_across_pages misplaced in TESTS_PART_0 ⭐
- **File:** `scripts/test_round1_unit.py:436` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `978499b` (no session tag)
- **Evidence:**
  ```python
  TESTS_PART_0 = [
      test_merge_upgrades_no_email_to_email,
      test_merge_upgrades_unknown_to_verified,
      test_merge_fills_empty_title,
      test_merge_no_downgrade,
      test_merge_no_duplicate_append,
      test_merge_adds_new_name,
      test_extract_from_multiple_upgrades_across_pages,
  ]
  ```
- **Why it matters:** This test uses monkeypatch and exercises ce.extract_from_multiple, not _merge_contact_upgrade. Unused import at line 107 confirms incomplete refactor.
- **Suggested fix:** Move to TESTS_PART_1 and remove unused import.
- **Confidence:** medium

### `tools/activity_tracker.py`

### [MED-77] log_activity swallows all exceptions and returns None silently ⭐
- **File:** `tools/activity_tracker.py:222` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `a2ef953` (no session tag)
- **Evidence:**
  ```python
      except Exception as e:
          logger.error(f"log_activity failed: {e}")
  ```
- **Why it matters:** Callers get no signal when activities fail to persist. Transient Sheets errors silently drop rows.
- **Suggested fix:** Re-raise or return bool indicating success.
- **Confidence:** high

### [MED-78] set_goal uses truthy check on existing_row_idx ⭐
- **File:** `tools/activity_tracker.py:371` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `a2ef953` (no session tag)
- **Evidence:**
  ```python
          if existing_row_idx:
              # Update in place
              service.spreadsheets().values().update(
  ```
- **Why it matters:** existing_row_idx starts at 2 so 0 never occurs in practice, but 'if existing_row_idx:' is semantically wrong — falsy index would skip update and append duplicate.
- **Suggested fix:** Change to 'if existing_row_idx is not None:'.
- **Confidence:** medium

### [MED-79] scan_pandadoc_notifications dedup check absent in standalone callers ⭐
- **File:** `tools/activity_tracker.py:457` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `a2ef953` (no session tag)
- **Evidence:**
  ```python
  def scan_pandadoc_notifications(gas_bridge) -> list[dict]:
      """
      Search Gmail inbox for PandaDoc quote notification emails.
      Returns list of activity dicts (not yet logged — caller decides whether to log).
  
      Parses: opened / signed / rejected events.
      Subject lines look like:
        "[PandaDoc] Quote Opened: Austin ISD - CodeCombat Proposal"
        "[PandaDoc] Document Signed: ..."
      """
  ```
- **Why it matters:** Docstring says 'caller decides' without warning that callers other than sync_gmail_activities bypass dedup entirely.
- **Suggested fix:** Document dedup contract explicitly or extract shared dedup helper.
- **Confidence:** medium

### `tools/charter_prospector.py`

### [MED-82] build_csta_enrichment import inside per-CMO try/except swallows ImportError silently ⭐
- **File:** `tools/charter_prospector.py:128` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `3ea1be1` (no session tag)
- **Evidence:**
  ```python
          try:
              from tools.signal_processor import build_csta_enrichment
              full_notes, priority_bonus = build_csta_enrichment(name, state_code, full_notes)
              result = district_prospector.add_district(
  ```
- **Why it matters:** If signal_processor fails to import, every CMO iteration lands in errors with cryptic ImportError string, masking root cause with N misleading error entries.
- **Suggested fix:** Hoist the import to top of queue_charter_cmos outside the loop and try/except.
- **Confidence:** high

### `tools/compliance_gap_scanner.py`

### [MED-83] Serper HTTP status not checked — 401/429 silently treated as zero results ⭐
- **File:** `tools/compliance_gap_scanner.py:146` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `442d7cb` (no session tag)
- **Evidence:**
  ```python
              data = resp.json()
              for item in data.get("organic", [])[:max_per_query]:
  ```
- **Why it matters:** resp.json() called without status check. 401/429 returns error body, silently yielding empty results.
- **Suggested fix:** Add resp.raise_for_status() before resp.json().
- **Confidence:** high

### [MED-84] Rate-limit stamp written before scan completes — blocks retries on zero-PDF runs ⭐
- **File:** `tools/compliance_gap_scanner.py:306` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `a846137` (no session tag)
- **Evidence:**
  ```python
      _LAST_SCAN[state_upper] = datetime.now()
  
      # Step 1: find candidate PDFs
      pdf_hits = _serper_pdf_urls(state_upper)
      logger.info(f"F9 {state_upper}: {len(pdf_hits)} candidate PDFs")
      if not pdf_hits:
          return {
  ```
- **Why it matters:** _LAST_SCAN stamped unconditionally. Failed Serper discovery still locks out state for 24h.
- **Suggested fix:** Move _LAST_SCAN write to after confirming pdf_hits is non-empty.
- **Confidence:** high

### [MED-85] extractions always appended even when parse error present ⭐
- **File:** `tools/compliance_gap_scanner.py:334` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `a996b32` (no session tag)
- **Evidence:**
  ```python
          if err:
              parse_errors.append({"url": url, "error": err})
          extractions.append({
              "url": url,
              "title": hit["title"],
              "districts": districts,
          })
  ```
- **Why it matters:** Failed extractions added to both parse_errors and extractions, inflating pdf_count.
- **Suggested fix:** Continue to next hit when err is non-None.
- **Confidence:** medium

### `tools/contact_extractor.py`

### [MED-86] CTE filter may drop non-CTE contacts ⭐
- **File:** `tools/contact_extractor.py:239` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b344cc2` (no session tag)
- **Evidence:**
  ```python
              try:
                  from agent.target_roles import is_relevant_cte_role
                  if not is_relevant_cte_role(contact["title"]):
                      logger.debug(f"Filtered irrelevant CTE: {contact['first_name']} {contact['last_name']} — {contact['title']}")
                      continue
              except ImportError:
                  pass  # target_roles not available, skip filter
  ```
- **Why it matters:** Filter applied to every contact regardless of CTE context. Risk of silently dropping non-CTE contacts (Superintendent, Principal) depending on is_relevant_cte_role behavior.
- **Suggested fix:** Guard: only apply filter if title contains CTE-related keyword, or confirm is_relevant_cte_role behavior for non-CTE roles.
- **Confidence:** medium

### [MED-87] detect_email_pattern imports EMAIL_PATTERNS but never uses it ⭐
- **File:** `tools/contact_extractor.py:359` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `6ec1bef` (no session tag)
- **Evidence:**
  ```python
  def detect_email_pattern(known_emails: list[str], domain: str) -> str | None:
      """
      Given a list of known emails from a district and the domain,
      detect which pattern they use.
      Returns the pattern string or None.
      """
      from agent.keywords import EMAIL_PATTERNS
  ```
- **Why it matters:** Function imports EMAIL_PATTERNS but uses hardcoded regex branches instead. Half-finished design.
- **Suggested fix:** Either iterate EMAIL_PATTERNS structurally or remove the dead import.
- **Confidence:** high

### `tools/cte_prospector.py`

### [MED-90] Wrong key used to extract skip reason from add_district result ⭐
- **File:** `tools/cte_prospector.py:154` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `ac2c4e4` (no session tag)
- **Evidence:**
  ```python
                      "reason": result.get("error", "unknown"),
  ```
- **Why it matters:** add_district returns {success, message, already_exists} — there is no 'error' key. result.get('error', 'unknown') always falls back to 'unknown', losing actual skip reason.
- **Suggested fix:** Change to result.get('message', 'unknown').
- **Confidence:** high

### `tools/sheets_writer.py`

### [MED-109] write_contacts has no exception handling around append calls ⭐
- **File:** `tools/sheets_writer.py:401` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `48d6cd7` (no session tag)
- **Evidence:**
  ```python
      # Append to Leads tab
      if leads_rows:
          service.spreadsheets().values().append(
              spreadsheetId=sheet_id,
              range=f"'{TAB_LEADS}'!A:K",
              valueInputOption="RAW",
              insertDataOption="INSERT_ROWS",
              body={"values": leads_rows}
          ).execute()
  ```
- **Why it matters:** Unhandled exception propagates. Caller receives no partial-success info.
- **Suggested fix:** Wrap in try/except, return error key.
- **Confidence:** medium

### [MED-110] CLAUDE.md documents log_research_job without cross_contam_dropped ⭐
- **File:** `tools/sheets_writer.py:543` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `48d6cd7` (no session tag)
- **Evidence:**
  ```python
  def log_research_job(
      district: str,
      state: str,
      layers_used: str,
      total_found: int,
      with_email: int,
      no_email: int,
      notes: str = "",
      cross_contam_dropped: int = 0,
  ):
  ```
- **Why it matters:** CLAUDE.md signature missing cross_contam_dropped. Callers won't know column exists, silently report 0.
- **Suggested fix:** Update CLAUDE.md signature.
- **Confidence:** high

### `tools/territory_data.py`

### [MED-114] Cross-state contamination via school_parent_keys in get_territory_gaps fallback ⭐
- **File:** `tools/territory_data.py:854` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `3c0af4f` (no session tag)
- **Evidence:**
  ```python
          use_accounts = active_accounts if active_accounts else all_active
  ```
- **Why it matters:** When target-state active_accounts is empty, fallback uses all_active (all states). Accounts without state value bypass filter, polluting school_parent_keys.
- **Suggested fix:** Remove all_active fallback or validate state before adding to school_parent_keys.
- **Confidence:** medium

### `agent/claude_brain.py`

### [MED-1] find_nearby_prospects tool schema missing 'required' key ⭐
- **File:** `agent/claude_brain.py:374` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `42e18ae` (no session tag)
- **Evidence:**
  ```python
          "input_schema": {
              "type": "object",
              "properties": {
                  "account_name": {
                      "type": "string",
                      "description": "Specific active account to search around (preferred). E.g. 'Leander ISD'",
                  },
  ```
- **Why it matters:** Every other tool in TOOLS includes a 'required' key; this one omits it. Some API versions reject tool schemas violating the object schema contract.
- **Suggested fix:** Add 'required': [] to the input_schema object.
- **Confidence:** high

### [MED-2] build_system_context duplicates build_memory_context logic instead of calling it ⭐
- **File:** `agent/claude_brain.py:491` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `567f596` (no session tag)
- **Evidence:**
  ```python
  def build_system_context(memory: MemoryManager) -> str:
      system = load_system_prompt()
  
      preferences = memory.load_preferences()
      if preferences:
          system += f"\n\n---\n## Learned Preferences\n{preferences}"
  
      recent = memory.load_recent_summary()
      if recent:
          system += f"\n\n---\n## Recent Activity Context\n{recent}"
  ```
- **Why it matters:** CLAUDE.md says memory injection goes through memory_manager.build_memory_context(), but this function calls load_preferences/load_recent_summary directly with different section headers. Silent divergence if build_memory_context adds new blocks.
- **Suggested fix:** Replace direct calls with memory.build_memory_context().
- **Confidence:** high

### `scripts/diocesan_drip.py`

### [MED-32] DRIP_DAYS hardcoded to 2026-04-13..16; silently stale after that window ⭐
- **File:** `scripts/diocesan_drip.py:87` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
  DRIP_DAYS: list[str] = [
      "2026-04-13",  # Mon — Batch 1
      "2026-04-14",  # Tue — Batch 2
      "2026-04-15",  # Wed — Batch 3
      "2026-04-16",  # Thu — Batch 4
  ]
  ```
- **Why it matters:** After 2026-04-16, --assign builds a plan with all day_bucket values in the past. --execute silently does nothing.
- **Suggested fix:** Derive DRIP_DAYS dynamically or validate in cmd_assign that days are not all in the past.
- **Confidence:** high

### `scripts/fetch_csta_roster.py`

### [MED-41] EXPLICIT_STATE_CHAPTER_URLS subdomain mismatch with REGIONAL_SUBDOMAIN_TO_STATE ⭐
- **File:** `scripts/fetch_csta_roster.py:142` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `529a919` (no session tag)
- **Evidence:**
  ```python
          "https://farnorthca.csteachers.org/",
  ```
- **Why it matters:** url_to_state_code extracts 'farnorthca' but map has 'far-north'. URL falls through to national bucket instead of CA.
- **Suggested fix:** Add 'farnorthca': 'CA' to REGIONAL_SUBDOMAIN_TO_STATE.
- **Confidence:** high

### [MED-42] haiku_extract silently returns [] on JSON parse failure ⭐
- **File:** `scripts/fetch_csta_roster.py:434` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `69ec3b8` (no session tag)
- **Evidence:**
  ```python
      except json.JSONDecodeError as e:
          print(f"  [warn] JSON parse failed for {label}: {e}", file=sys.stderr)
          return []
  ```
- **Why it matters:** Entire state bucket's extraction silently dropped. Caller can't distinguish empty state from parse failure.
- **Suggested fix:** Track failed-state count and report in final summary.
- **Confidence:** medium

### [MED-43] normalize_and_dedup merges on name alone — cross-state collision risk ⭐
- **File:** `scripts/fetch_csta_roster.py:648` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `529a919` (no session tag)
- **Evidence:**
  ```python
          key = csv_importer.normalize_name(name)
          state = (it.get("state") or "").strip().upper()
          if state and len(state) != 2:
              continue
          by_person[key] = dict(it)
  ```
- **Why it matters:** Common name like 'John Smith' in TX and CA treated as same person; second occurrence silently overwrites.
- **Suggested fix:** Build dedup key as (normalized_name, state_code).
- **Confidence:** medium

### `scripts/load_campaign.py`

### [MED-45] load_dotenv failure silently swallowed ⭐
- **File:** `scripts/load_campaign.py:68` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `afb8680` (no session tag)
- **Evidence:**
  ```python
  try:
      from dotenv import load_dotenv  # noqa: E402
  
      load_dotenv(REPO_ROOT / ".env")
  except Exception:
      pass
  ```
- **Why it matters:** Bare except catches both ImportError and runtime errors. Auth failures become opaque.
- **Suggested fix:** Narrow to ImportError; log warning if load_dotenv returns False.
- **Confidence:** high

### [MED-46] sequence_id_for inner function KeyError risk ⭐
- **File:** `scripts/load_campaign.py:384` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `09668c0` (no session tag)
- **Evidence:**
  ```python
      def sequence_id_for(contact: Contact) -> int:
          role = role_map[contact.email]
          return int(sequences[role]["id"])
  ```
- **Why it matters:** role_map[email] and sequences[role] both raise bare KeyError. Filter loop uses .get with default, creating inconsistency.
- **Suggested fix:** Use .get with explicit guards and descriptive ValueError.
- **Confidence:** medium

### [MED-47] mailbox_id hardcoded to 11 ⭐
- **File:** `scripts/load_campaign.py:396` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `09668c0` (no session tag)
- **Evidence:**
  ```python
      plans = build_load_plan(
          contacts_with_seq,
          sequence_id_for=sequence_id_for,
          days=[d.isoformat() for d in campaign.drip_days],
          mailbox_id=11,
          tags_for=tags_for,
          group_key=lambda c: c.company,
      )
  ```
- **Why it matters:** Magic number with no campaign-level override. Wrong mailbox silently loads all contacts incorrectly.
- **Suggested fix:** Add mailbox_id field to Campaign dataclass or CLI --mailbox-id arg.
- **Confidence:** high

### `scripts/test_export_sequence_for_editing.py`

### [MED-71] Shallow copy of FAKE_SEQ mutates shared fixture across tests ⭐
- **File:** `scripts/test_export_sequence_for_editing.py:159` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `a530aac` (no session tag)
- **Evidence:**
  ```python
      fake_empty = dict(FAKE_SEQ)
      fake_empty["steps"] = []
  ```
- **Why it matters:** dict() is shallow copy. Nested step dicts shared as references. Test ordering can cause false passes/fails.
- **Suggested fix:** Use copy.deepcopy(FAKE_SEQ) or define separate constants per test.
- **Confidence:** medium

### `tools/campaign_file.py`

### [MED-80] num_steps not validated against actual parsed step count ⭐
- **File:** `tools/campaign_file.py:222` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `f638168` (no session tag)
- **Evidence:**
  ```python
          target_role_label, num_steps, steps_block = _parse_variant_meta(variant_block)
          steps = _parse_steps(steps_block, intervals_seconds)
  
          if not steps:
              raise CampaignFileError(f"variant '{role}' has no steps")
  
          variants[role] = CampaignVariant(
              role=role,
              target_role_label=target_role_label,
              num_steps=num_steps,
  ```
- **Why it matters:** Declared num_steps stored verbatim without comparing to len(steps). Callers trusting num_steps behave incorrectly.
- **Suggested fix:** Assert len(steps) == num_steps or compute from len(steps).
- **Confidence:** high

### [MED-81] datetime.datetime silently accepted as date in _coerce_date ⭐
- **File:** `tools/campaign_file.py:252` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `f638168` (no session tag)
- **Evidence:**
  ```python
      if isinstance(v, date):
          return v
  ```
- **Why it matters:** datetime.datetime is a date subclass; YAML parses '2026-04-21 00:00:00' as datetime and it passes isinstance check. Round-trip corrupts the data.
- **Suggested fix:** Add explicit datetime.datetime check: return v.date() for datetimes.
- **Confidence:** high

### `tools/csv_importer.py`

### [MED-88] fuzzy_match_name single-token query vs multi-token candidate silently misses ⭐
- **File:** `tools/csv_importer.py:337` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `9d39387` (no session tag)
- **Evidence:**
  ```python
          shorter_tokens = query_tokens if len(query_tokens) <= len(cand_tokens) else cand_tokens
          longer_tokens = cand_tokens if len(query_tokens) <= len(cand_tokens) else query_tokens
          if shorter_tokens and len(shorter_tokens) >= 2 and shorter_tokens <= longer_tokens:
  ```
- **Why it matters:** Token-subset branch requires len>=2. Single-token containment requires both sides =1. Single-token query vs multi-token candidate falls through to Jaccard (~1/3), missing valid matches like 'irvine' vs 'irvine unified'.
- **Suggested fix:** Add single-token-in-multi-token containment branch before Jaccard.
- **Confidence:** high

### [MED-89] fuzzy_match_name first subset match wins on tie ⭐
- **File:** `tools/csv_importer.py:337` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `9d39387` (no session tag)
- **Evidence:**
  ```python
          if shorter_tokens and len(shorter_tokens) >= 2 and shorter_tokens <= longer_tokens:
              # Full token containment with 2+ tokens — strong match
              score = 0.95
              if score > best_score:
                  best_score = score
                  best_key = cand_key
              continue
  ```
- **Why it matters:** Strict > means later equal-scoring subset match never replaces earlier. Wrong candidate may win depending on dict iteration order.
- **Suggested fix:** Use >= or prefer smaller token set on tie.
- **Confidence:** medium

### `agent/target_roles.py`

### [MED-9] IT infrastructure filter uses broad single-word 'server' keyword ⭐
- **File:** `agent/target_roles.py:344` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `4e139a2` (no session tag)
- **Evidence:**
  ```python
      if any(kw in title_lower for kw in IT_INFRA_KEYWORDS):
  ```
- **Why it matters:** Bare 'server' entry in IT_INFRA_KEYWORDS is too broad and could filter legitimate roles without education qualifier rescue.
- **Suggested fix:** Remove bare 'server' entry or require word-boundary match.
- **Confidence:** medium

### `scripts/ab_analyze.py`

### [MED-13] per-row delta_pct silently shows 0 when v1_cost is zero ⭐
- **File:** `scripts/ab_analyze.py:46` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `21d9b3b` (no session tag)
- **Evidence:**
  ```python
          delta_pct = ((v2_cost - v1_cost) / v1_cost * 100) if v1_cost else 0
  ```
- **Why it matters:** When v1_cost is 0, delta_pct silently prints as '+0.0%' indistinguishable from real 0% delta.
- **Suggested fix:** Use float('nan') or sentinel to signal missing data.
- **Confidence:** medium

### `scripts/ab_research_engine.py`

### [MED-14] _layer_fired skipped_layers membership uses substring match ⭐
- **File:** `scripts/ab_research_engine.py:65` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `21d9b3b` (no session tag)
- **Evidence:**
  ```python
  def _layer_fired(result: dict, layer_tag: str) -> bool:
      layers_used = result.get("layers_used") or ""
      skipped = set(result.get("skipped_layers") or [])
      return layer_tag in layers_used and not any(
          layer_tag.split(":")[0] in s for s in skipped
      )
  ```
- **Why it matters:** Substring match means 'L16' matches 'L160' or 'L16_alt'. Prefix collisions under-report queries.
- **Suggested fix:** Use exact set membership.
- **Confidence:** medium

### `scripts/audit_c4_prospects.py`

### [MED-15] Engagement signal 'has_replies' over-counts due to fragile substring check ⭐
- **File:** `scripts/audit_c4_prospects.py:238` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
          if "replies" in notes and "no reply" not in notes:
              has_replies += 1
  ```
- **Why it matters:** Notes like '0 replies' or 'no replies' (plural) contain 'replies' but aren't positive engagement. Guard only catches 'no reply' singular.
- **Suggested fix:** Parse numeric count or add 'no replies' to guard.
- **Confidence:** medium

### `scripts/audit_leads_cross_contamination.py`

### [MED-16] target_hint length guard < 5 too strict ⭐
- **File:** `scripts/audit_leads_cross_contamination.py:58` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
      if len(target_hint) < 5:
          return ("ambiguous", f"target_hint too short: {target_hint!r}")
  ```
- **Why it matters:** 4-char hints (eths, cusd) classified ambiguous. Short acronym hints create false negatives.
- **Suggested fix:** Lower threshold or document rationale.
- **Confidence:** medium

### [MED-17] email_local_root derived from email_domain silently wrong for sub-domains ⭐
- **File:** `scripts/audit_leads_cross_contamination.py:63` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
      email_local_root = email_domain.split(".")[0] if email_domain else ""
  ```
- **Why it matters:** For 'user@mail.district.k12.il.us' returns 'mail' not the meaningful root. Breaks _GENERIC_EMAIL_ROOTS checks.
- **Suggested fix:** Use second-to-last label.
- **Confidence:** medium

### [MED-18] Broad except swallows all Google Doc creation errors ⭐
- **File:** `scripts/audit_leads_cross_contamination.py:266` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
      except Exception as e:
          print(f"Google Doc creation error: {e}")
  ```
- **Why it matters:** Auth or GAS bridge failures silently produce no doc with exit 0.
- **Suggested fix:** Re-raise non-transient errors or return non-zero exit code.
- **Confidence:** medium

### `scripts/bug5_cleanup_lackland_test.py`

### [MED-19] headers.index raises ValueError if 'District Name' absent ⭐
- **File:** `scripts/bug5_cleanup_lackland_test.py:39` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
  dn_idx = headers.index("District Name")
  ```
- **Why it matters:** Unhandled ValueError with no actionable context.
- **Suggested fix:** Wrap in try/except with descriptive error message.
- **Confidence:** medium

### [MED-20] Research Log deletion not date-filtered despite 'today only' docstring ⭐
- **File:** `scripts/bug5_cleanup_lackland_test.py:49` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
  log_lackland = [i + 1 for i, row in enumerate(log_values[1:], start=1)
                  if len(row) > 1 and "lackland" in row[1].lower()]
  ```
- **Why it matters:** Docstring says 'today only' but no date check. Historical rows destroyed permanently.
- **Suggested fix:** Add date filter using today's date.
- **Confidence:** high

### [MED-21] No guard for leads_tab_id or log_tab_id == None ⭐
- **File:** `scripts/bug5_cleanup_lackland_test.py:55` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
  for r in sorted(lackland_rows, reverse=True):
      requests.append({
          "deleteDimension": {
              "range": {"sheetId": leads_tab_id, "dimension": "ROWS",
  ```
- **Why it matters:** If tab not found, IDs are None; silently embedded in payload causing cryptic API error or wrong-sheet operation.
- **Suggested fix:** Assert tab IDs are not None after discovery.
- **Confidence:** high

### `scripts/bug5_dryrun.py`

### [MED-22] f-string on line 276 has no placeholders ⭐
- **File:** `scripts/bug5_dryrun.py:276` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `148aca6` (no session tag)
- **Evidence:**
  ```python
          sys.exit(f"Oracle files missing. Run scripts/bug5_phase0_scan.py first.")
  ```
- **Why it matters:** F541 flagged. Fragile — future variable embedding would silently fail if f- removed.
- **Suggested fix:** Drop the f prefix.
- **Confidence:** high

### `scripts/bug5_phase0_scan.py`

### [MED-23] .env parse strips quotes non-robustly ⭐
- **File:** `scripts/bug5_phase0_scan.py:12` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
      os.environ.setdefault(k.strip(), v.strip().strip("'").strip('"'))
  ```
- **Why it matters:** strip() removes all matching quote characters from both ends, not just matching pairs. Can mangle values with leading/trailing quotes.
- **Suggested fix:** Use python-dotenv or check matching quote pair.
- **Confidence:** medium

### [MED-24] urllib.parse imported inside loop body ⭐
- **File:** `scripts/bug5_phase0_scan.py:102` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
      from urllib.parse import urlparse
      src_host = urlparse(src).netloc.lower().replace("www.", "")
  ```
- **Why it matters:** Import in per-row loop body. Structurally unclean.
- **Suggested fix:** Move to top-level imports.
- **Confidence:** high

### `scripts/bug5_smoke_verify.py`

### [MED-25] cross_contam_dropped value not validated against expected count ⭐
- **File:** `scripts/bug5_smoke_verify.py:55` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
  if "Cross-Contam Dropped" in headers:
      idx = headers.index("Cross-Contam Dropped")
      val = latest[idx] if idx < len(latest) else None
      print(f"\n✅ cross_contam_dropped column present: value = {val!r}")
  else:
      print("\n❌ cross_contam_dropped column MISSING from header")
  ```
- **Why it matters:** Script docstring says verify equals 9 but only prints value. Wrong value silently prints green check.
- **Suggested fix:** Assert val == expected count; exit 1 on mismatch.
- **Confidence:** high

### [MED-26] date_idx assigned but never used — date filter silently absent ⭐
- **File:** `scripts/bug5_smoke_verify.py:69` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `b809198` (no session tag)
- **Evidence:**
  ```python
  date_idx = lead_headers.index("Date Found")
  ```
- **Why it matters:** Script meant to restrict to today's run but date check is absent. Stale historical rows pass.
- **Suggested fix:** Use date_idx to filter to today or remove the lookup.
- **Confidence:** high

### `scripts/create_c4_sequences.py`

### [MED-27] text_to_html joins with space losing blank-line paragraph breaks ⭐
- **File:** `scripts/create_c4_sequences.py:79` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
      html_parts = []
      for line in lines:
          line = line.strip()
          if not line:
              html_parts.append("<br><br>")
          else:
              html_parts.append(line)
      return " ".join(html_parts)
  ```
- **Why it matters:** Joining with space produces spaces around <br><br> tags.
- **Suggested fix:** Join with empty string or handle separators explicitly.
- **Confidence:** medium

### [MED-28] Existing template ID not preflighted ⭐
- **File:** `scripts/create_c4_sequences.py:213` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
      {
          "subject": "CodeCombat's Comprehensive K-12 Suite",
          "body": "",  # unused
          "interval": INTERVAL_WEEKLY,
          "use_existing_template": EXISTING_TEMPLATE_ID,
      },
  ```
- **Why it matters:** No validation that EXISTING_TEMPLATE_ID=43784 is accessible. If deleted, silent broken sequence.
- **Suggested fix:** Preflight GET /templates/43784 before sequence creation.
- **Confidence:** medium

### `scripts/delete_zzz_sentinels.py`

### [MED-29] Row range capped at 3000 may silently miss sentinels beyond ⭐
- **File:** `scripts/delete_zzz_sentinels.py:33` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
      spreadsheetId=sheet_id, range=f"'{TAB}'!A1:T3000"
  ```
- **Why it matters:** Hardcoded 3000. Rows beyond silently skipped.
- **Suggested fix:** Use unbounded A:T or check row count.
- **Confidence:** high

### [MED-30] No dry-run or confirmation gate before destructive bulk delete ⭐
- **File:** `scripts/delete_zzz_sentinels.py:53` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
  # Build deleteDimension requests — reverse order to preserve indices
  requests = []
  for idx, _name in sorted(zzz_rows, reverse=True):
  ```
- **Why it matters:** Immediate delete with no --dry-run flag. Misidentified pattern or wrong sheet = permanent data loss.
- **Suggested fix:** Add --dry-run flag via argparse.
- **Confidence:** medium

### `scripts/enrich_c4_pass2.py`

### [MED-33] get_row_idx() defined but never called — dead code ⭐
- **File:** `scripts/enrich_c4_pass2.py:477` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
      def get_row_idx(prospect):
          """Find the 1-indexed row number in the sheet for this prospect."""
          email = prospect.get("Email", "").strip().lower()
          name_key = prospect.get("Name Key", "").strip()
          for i, row in enumerate(pq_rows[1:], start=2):
              padded = row + [""] * (len(pq_headers) - len(row))
              p = dict(zip(pq_headers, padded))
  ```
- **Why it matters:** Function defined but c4_row_indices is used instead. Name Key fallback silently skipped.
- **Suggested fix:** Remove or integrate Name Key fallback into c4_row_indices build.
- **Confidence:** high

### [MED-34] Bare except-pass swallows all Serper future errors ⭐
- **File:** `scripts/enrich_c4_pass2.py:905` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
                  try:
                      results = future.result()
                      if results:
                          title_search_results[item["email"]] = {"results": results, "item": item}
                  except Exception:
                      pass
  ```
- **Why it matters:** No log message. Can't diagnose systematic failures.
- **Suggested fix:** Log warnings for caught exceptions.
- **Confidence:** high

### [MED-35] chr(65+col) breaks for columns beyond Z ⭐
- **File:** `scripts/enrich_c4_pass2.py:972` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
              col_letter = chr(65 + state_col)  # A
              sheet_updates.append({
                  "range": f"'Prospecting Queue'!{col_letter}{row_idx}",
                  "values": [[upd["State"]]],
              })
  ```
- **Why it matters:** Only works for columns 0-25. chr(91) is '['. Sheet with >26 columns silently writes to wrong cell.
- **Suggested fix:** Use proper column-to-letter converter.
- **Confidence:** high

### `scripts/enrich_c4_titles.py`

### [MED-36] urllib.parse imported inside loop body twice ⭐
- **File:** `scripts/enrich_c4_titles.py:148` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
          import urllib.parse
          parsed = urllib.parse.urlparse(next_link)
          next_params = dict(urllib.parse.parse_qsl(parsed.query))
          params.update(next_params)
  ```
- **Why it matters:** Import inside while loops at lines 148 and 444. Structurally unclean.
- **Suggested fix:** Move to top-level imports.
- **Confidence:** high

### [MED-37] claude_classify_roles swallows exceptions and returns [] with no retry ⭐
- **File:** `scripts/enrich_c4_titles.py:270` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
      except Exception as e:
          logger.error(f"Claude classification error: {e}")
          return []
  ```
- **Why it matters:** Entire batch of 25 silently dropped on transient failures. Layer 4 = last chance.
- **Suggested fix:** Add retry with exponential backoff; log batch size on failure.
- **Confidence:** high

### `scripts/f4_serper_replay.py`

### [MED-39] Serper response status never checked ⭐
- **File:** `scripts/f4_serper_replay.py:89` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `54e7fed` (no session tag)
- **Evidence:**
  ```python
              resp = httpx.post(
                  SERPER_URL,
                  headers={"X-API-Key": _SERPER_KEY, "Content-Type": "application/json"},
                  json={"q": query, "num": 10, "tbs": "qdr:y"},
                  timeout=20.0,
              )
              data = resp.json()
  ```
- **Why it matters:** No raise_for_status. 401/429/5xx silently returns JSON error body; empty organic looks like real empty state.
- **Suggested fix:** Add resp.raise_for_status() after httpx.post.
- **Confidence:** high

### [MED-40] combined_text truncated mid-snippet ⭐
- **File:** `scripts/f4_serper_replay.py:169` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `54e7fed` (no session tag)
- **Evidence:**
  ```python
      combined_text = combined_text[:20000]
  ```
- **Why it matters:** Slice has no snippet-boundary awareness. LLM may hallucinate or miss districts on truncated final snippet.
- **Suggested fix:** Accumulate per-snippet with early exit at budget.
- **Confidence:** medium

### `scripts/level2_integration_check.py`

### [MED-44] result.get() silently produces None in print — crash if result itself is None ⭐
- **File:** `scripts/level2_integration_check.py:69` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `978499b` (no session tag)
- **Evidence:**
  ```python
      print(
          f"Done: total={condensed['total']} verified={condensed['verified']} "
          f"with_email={condensed['with_email']} queries={condensed['queries_used']} "
          f"elapsed={condensed['elapsed_seconds']}s",
          flush=True,
      )
  ```
- **Why it matters:** If job.run() returns None, result.get(...) raises AttributeError, crashing without writing output.
- **Suggested fix:** Guard against None result from job.run().
- **Confidence:** medium

### `scripts/phase1_ground_truth_lackland.py`

### [MED-48] Date Added search checks every cell instead of column M ⭐
- **File:** `scripts/phase1_ground_truth_lackland.py:151` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
      for i, row in enumerate(data_rows):
          for val in row:
              if isinstance(val, str) and val.startswith("2026-04-10 00:5"):
  ```
- **Why it matters:** Timestamps in other cells produce false-positive hits, polluting ground-truth output.
- **Suggested fix:** Check only row[12] (canonical Date Added column).
- **Confidence:** medium

### `scripts/phase1c_scramble_block.py`

### [MED-49] Scrambled date index 14 unverified ⭐
- **File:** `scripts/phase1c_scramble_block.py:44` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
          return "SCRAMBLED", padded[2], padded[14]
  ```
- **Why it matters:** Comment asserts date at idx 14 but scrambled rows may not match. Empty dates silently returned if wrong.
- **Suggested fix:** Verify empirically against sheet schema; add fallback debug print for UNKNOWN.
- **Confidence:** low

### [MED-50] Transition loop row number may be off by one relative to actual sheet rows ⭐
- **File:** `scripts/phase1c_scramble_block.py:75` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
      print(f"  row {i+1:>4}: {fp:<10} │ strat={strat:<25} │ date={date:<20} │ {name}")
  ```
- **Why it matters:** Enumerate starts from 0 and prints i+1, but rows[0] is header so rows[i] corresponds to sheet row i+1. Reported row numbers may be misleading.
- **Suggested fix:** Audit mapping; use i+2 if skipping header.
- **Confidence:** medium

### `scripts/phase1d_local_sentinel.py`

### [MED-51] Cleanup instruction references script that may not exist ⭐
- **File:** `scripts/phase1d_local_sentinel.py:102` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  print(f"Clean up: run `python3 scripts/phase1d_cleanup.py {sentinel_row_num}` (script below)")
  ```
- **Why it matters:** Refers to phase1d_cleanup.py but says '(script below)' implying it was meant to be generated/appended. If missing, sentinel row persists.
- **Suggested fix:** Verify existence or embed cleanup logic.
- **Confidence:** medium

### `scripts/phase1e_executor_test.py`

### [MED-52] .env parsing has no error handling ⭐
- **File:** `scripts/phase1e_executor_test.py:11` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  for line in env_path.read_text().splitlines():
      if "=" in line and not line.startswith("#"):
          k, v = line.split("=", 1)
  ```
- **Why it matters:** FileNotFoundError causes silent abort before sentinel write.
- **Suggested fix:** Wrap read in try/except.
- **Confidence:** medium

### `scripts/phase1g_railway_env_sentinel.py`

### [MED-53] KeyError on missing env vars ⭐
- **File:** `scripts/phase1g_railway_env_sentinel.py:30` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
      json.loads(os.environ["GOOGLE_SERVICE_ACCOUNT_JSON"]),
  ```
- **Why it matters:** Raw KeyError with no diagnostic.
- **Suggested fix:** Use os.environ.get with sys.exit message.
- **Confidence:** high

### [MED-54] Sentinel search scans including header row, off-by-one ⭐
- **File:** `scripts/phase1g_railway_env_sentinel.py:38` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  for i, row in enumerate(resp.get("values", [])):
      if SENTINEL in str(row):
  ```
- **Why it matters:** i=0 prints 'row 1' which is header. Fragile assumption.
- **Suggested fix:** Skip header explicitly.
- **Confidence:** medium

### `scripts/phase2_fingerprint_audit.py`

### [MED-55] Unhandled FileNotFoundError if .env missing ⭐
- **File:** `scripts/phase2_fingerprint_audit.py:15` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  for line in env_path.read_text().splitlines():
  ```
- **Why it matters:** Raw FileNotFoundError with no hint about missing .env.
- **Suggested fix:** Wrap in try/except with clear message.
- **Confidence:** medium

### [MED-56] Hardcoded '2026-' year in date detection breaks after 2026 ⭐
- **File:** `scripts/phase2_fingerprint_audit.py:91` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
          if idx < len(padded) and padded[idx].startswith("2026-"):
  ```
- **Why it matters:** Silently skips non-2026 dates, leaving oldest_by_sig/newest_by_sig empty.
- **Suggested fix:** Use regex r'^\d{4}-\d{2}-\d{2}'.
- **Confidence:** high

### `scripts/phase2b_sample_fingerprints.py`

### [MED-57] .env value strip does not remove surrounding quotes ⭐
- **File:** `scripts/phase2b_sample_fingerprints.py:11` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
          k, v = line.split("=", 1)
          os.environ.setdefault(k.strip(), v.strip())
  ```
- **Why it matters:** KEY="value" leaves literal quotes in env var, breaks json.loads downstream.
- **Suggested fix:** Strip surrounding quotes.
- **Confidence:** medium

### `scripts/repair_scrambled_queue_rows.py`

### [MED-58] .env parsing has no FileNotFoundError guard ⭐
- **File:** `scripts/repair_scrambled_queue_rows.py:40` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `5990d8a` (no session tag)
- **Evidence:**
  ```python
  for line in env_path.read_text().splitlines():
      if "=" in line and not line.startswith("#"):
          k, v = line.split("=", 1)
  ```
- **Why it matters:** Missing .env file produces opaque traceback.
- **Suggested fix:** Wrap read in try/except.
- **Confidence:** medium

### `scripts/s60_schedule_lookup.py`

### [MED-62] Pagination drops first page params on subsequent pages ⭐
- **File:** `scripts/s60_schedule_lookup.py:58` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `846aaed` (no session tag)
- **Evidence:**
  ```python
          from urllib.parse import urlparse, parse_qs
          qs = parse_qs(urlparse(next_url).query)
          p = {k: v[0] for k, v in qs.items()}
          result = oc._api_get("/sequences", p)
  ```
- **Why it matters:** links.next URL may not preserve owner filter. All sequences returned instead of filtered.
- **Suggested fix:** Merge original params dict with parsed page params.
- **Confidence:** medium

### `scripts/s60_test_target_lookup.py`

### [MED-63] target_tokens filters len>3, drops district abbreviations ⭐
- **File:** `scripts/s60_test_target_lookup.py:63` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** Session 60 (`616e184`)
- **Evidence:**
  ```python
      target_tokens = set(t for t in target_key.split() if len(t) > 3)
  ```
- **Why it matters:** Drops 'isd', 'usd', 'cps' — load-bearing parts of district names.
- **Suggested fix:** Use dedicated stopword list instead of blanket length cutoff.
- **Confidence:** low

### [MED-64] Sheets range hardcoded with column indices 4 and 5 ⭐
- **File:** `scripts/s60_test_target_lookup.py:107` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** Session 60 (`616e184`)
- **Evidence:**
  ```python
  result = svc.spreadsheets().values().get(
      spreadsheetId=sheet_id, range="'Research Log'!A2:I"
  ).execute()
  ```
- **Why it matters:** Hard-coded column indices break silently on schema drift.
- **Suggested fix:** Fetch header row, build col-name-to-index map.
- **Confidence:** medium

### `scripts/s60_verify_test_targets.py`

### [MED-65] lookup_district_enrollment return value compared to 0 may crash on None ⭐
- **File:** `scripts/s60_verify_test_targets.py:28` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** Session 60 (`616e184`)
- **Evidence:**
  ```python
      enrollment = territory_data.lookup_district_enrollment(name, state)
      if enrollment > 0:
  ```
- **Why it matters:** None return raises TypeError: '>' not supported between NoneType and int.
- **Suggested fix:** Guard with 'if enrollment is not None and enrollment > 0'.
- **Confidence:** medium

### `scripts/telethon_auth.py`

### [MED-66] Phone code hash file written to CWD, not session file location ⭐
- **File:** `scripts/telethon_auth.py:29` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
      Path(".telethon_code_hash").write_text(r.phone_code_hash)
  ```
- **Why it matters:** If script is invoked from different directories for send and verify phases, the hash file can't be found.
- **Suggested fix:** Anchor path to absolute location alongside session file.
- **Confidence:** high

### [MED-67] IndexError if 'verify' called without code argument ⭐
- **File:** `scripts/telethon_auth.py:54` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
      elif cmd == "verify":
          code = sys.argv[2]
  ```
- **Why it matters:** Unguarded IndexError with no helpful message.
- **Suggested fix:** Check len(sys.argv) > 2 first.
- **Confidence:** high

### `scripts/test_campaign_file.py`

### [MED-68] sleep_seconds round-trip check uses wrong attribute ⭐
- **File:** `scripts/test_campaign_file.py:90` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `f638168` (no session tag)
- **Evidence:**
  ```python
      _check("round-trip sleep_seconds", reparsed.sleep_seconds == original.sleep_seconds)
  ```
- **Why it matters:** Frontmatter declares sleep_seconds_min and sleep_seconds_max as separate fields. If dataclass uses separate fields, this check silently compares wrong attribute.
- **Suggested fix:** Verify Campaign dataclass structure; split into min/max checks if applicable.
- **Confidence:** medium

### `scripts/test_diocesan_drip.py`

### [MED-69] Tautological assert makes 'EST' rejection test a no-op ⭐
- **File:** `scripts/test_diocesan_drip.py:52` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
      assert is_valid_iana_timezone("EST") is False or is_valid_iana_timezone("EST") is True
  ```
- **Why it matters:** Assertion always True. If is_valid_iana_timezone('EST') silently started accepting 'EST', test would still pass.
- **Suggested fix:** Assert definite expected value: is_valid_iana_timezone('EST') is False.
- **Confidence:** high

### [MED-70] verified-first ordering test may pass spuriously due to day-bucket sort ⭐
- **File:** `scripts/test_diocesan_drip.py:224` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
      phil_plans = [p for p in plans if p.contact.diocese_or_group == "Archdiocese of Philadelphia"]
      # Phil contacts are Alice(VERIFIED), Bob(INFERRED), Carol(VERIFIED)
      # Sorted: Alice (VERIFIED), Carol (VERIFIED), Bob (INFERRED)
      order = [p.contact.first_name for p in phil_plans]
      verified_indices = [i for i, p in enumerate(phil_plans) if p.contact.confidence_rank() == 3]
      inferred_indices = [i for i, p in enumerate(phil_plans) if p.contact.confidence_rank() == 1]
  ```
- **Why it matters:** build_load_plan final-sorts by (day_bucket, diocese, email). With 3 contacts on 3 days, order incidentally preserves. If config changes to overlap, email alphabetical tiebreak could put INFERRED before VERIFIED.
- **Suggested fix:** Group by day_bucket and verify ordering within each day's subset.
- **Confidence:** medium

### `scripts/test_load_campaign.py`

### [MED-72] patch target load_campaign.state_path may not be a module attribute ⭐
- **File:** `scripts/test_load_campaign.py:367` @ `339c92c`
- **Category:** Amnesia / drift
- **Introduced in:** `6ea3b29` (no session tag)
- **Evidence:**
  ```python
               patch.object(load_campaign, "state_path", return_value=state_path):
  ```
- **Why it matters:** If state_path is computed locally inside cmd_create, patch has no effect and test writes to real path.
- **Suggested fix:** Verify state_path is module-level function or patch lower-level I/O helpers.
- **Confidence:** medium

### `scripts/test_role_classifier.py`

### [MED-73] MockClient.create never asserts model string ⭐
- **File:** `scripts/test_role_classifier.py:31` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `27e2243` (no session tag)
- **Evidence:**
  ```python
      def create(self, *, model: str, max_tokens: int, temperature: float, messages: list) -> object:
  ```
- **Why it matters:** Mock ignores model arg. If role_classifier changes model, tests silently pass while missing model drift regression.
- **Suggested fix:** Store model in self.last_model; assert against expected Haiku string.
- **Confidence:** medium

### `scripts/tg_send.py`

### [MED-76] KeyError on missing env vars produces unhelpful traceback ⭐
- **File:** `scripts/tg_send.py:12` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `746e1e7` (no session tag)
- **Evidence:**
  ```python
  API_ID = int(os.environ["TELEGRAM_API_ID"])
  API_HASH = os.environ["TELEGRAM_API_HASH"]
  ```
- **Why it matters:** Raw KeyError with no actionable guidance. int() on bad value also opaque.
- **Suggested fix:** Use os.environ.get with explicit check and sys.exit.
- **Confidence:** medium

### `tools/daily_call_list.py`

### [MED-91] No-districts fast path calls _rank_and_select without all_leads — backfill never runs ⭐
- **File:** `tools/daily_call_list.py:56` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `685312f` (no session tag)
- **Evidence:**
  ```python
              cards = _rank_and_select(
                  [{"lead": l, "district_info": None} for l in leads if l.get("Email")],
                  max_contacts=max_contacts,
              )
  ```
- **Why it matters:** _rank_and_select backfill only triggers when all_leads provided. In no-districts path, backfill never fires. Leads without email silently dropped before ranking.
- **Suggested fix:** Pass all_leads=leads in the no-districts call.
- **Confidence:** high

### `tools/prospect_loader.py`

### [MED-99] append_audit missing os.makedirs guard for empty dirname ⭐
- **File:** `tools/prospect_loader.py:224` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
  def append_audit(path: str, record: dict) -> None:
      os.makedirs(os.path.dirname(path), exist_ok=True)
  ```
- **Why it matters:** os.path.dirname('audit.jsonl') returns '', and os.makedirs('', exist_ok=True) raises on some platforms. write_state_atomic has the guard; append_audit doesn't.
- **Suggested fix:** Apply same dir_path = os.path.dirname(path) or '.' guard.
- **Confidence:** high

### [MED-100] dry_run mode does not write state — reprocesses all contacts on every run ⭐
- **File:** `tools/prospect_loader.py:326` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
          if dry_run:
              logger.info("  dry_run: would POST")
              summary["processed"] += 1
              continue
  ```
- **Why it matters:** State never updated in dry_run so already_done count always 0. Inaccurate workload picture.
- **Suggested fix:** Document intentional read-only behavior or persist a dry_run_done sentinel.
- **Confidence:** medium

### [MED-101] Membership check exception silently continues — could create duplicate sequenceStates ⭐
- **File:** `tools/prospect_loader.py:395` @ `339c92c`
- **Category:** Error handling
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
          except Exception as e:
              logger.warning(f"  membership check error (continuing): {e}")
  ```
- **Why it matters:** Transient /sequenceStates GET failure leaves already_in_seq=False and proceeds to add_prospect_to_sequence. Outreach accepts duplicate sequenceStates, creating duplicates.
- **Suggested fix:** On check failure, mark plan as failed or conservatively treat as already_in_seq=True.
- **Confidence:** high

### `tools/role_classifier.py`

### [MED-107] Haiku response parsing IndexError on punctuation-only responses ⭐
- **File:** `tools/role_classifier.py:132` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `27e2243` (no session tag)
- **Evidence:**
  ```python
      token = re.sub(r"[^a-z]", "", raw.split()[0] if raw else "")
  ```
- **Why it matters:** raw.split() returns empty list for whitespace-only strings; raw.split()[0] raises IndexError. Guard only covers empty string.
- **Suggested fix:** Check raw.split() truthiness before indexing.
- **Confidence:** high

### [MED-108] Cache stores raw title but key uses normalized title ⭐
- **File:** `tools/role_classifier.py:173` @ `339c92c`
- **Category:** Correctness
- **Introduced in:** `27e2243` (no session tag)
- **Evidence:**
  ```python
      key = _cache_key(title)
      cached = cache.get(key)
      if cached and cached.get("bucket") in VALID_ROLES:
          return cached["bucket"]
  ```
- **Why it matters:** Inconsistency between stored title metadata and key derivation breaks audit traceability.
- **Suggested fix:** Store _normalize_title(title) in the cache entry.
- **Confidence:** medium

### `tools/timezone_lookup.py`

### [MED-117] Tennessee default is Eastern but majority-population is Central ⭐
- **File:** `tools/timezone_lookup.py:62` @ `339c92c`
- **Category:** State / data integrity
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
      "TN": "America/New_York",          # majority-ET; western is CT
  ```
- **Why it matters:** Comment says 'majority-ET' but Nashville and bulk of population are in Central. If wrong, every TN prospect gets wrong IANA zone before Outreach POST.
- **Suggested fix:** Verify against Census/NCES data; flip to America/Chicago if Central dominates.
- **Confidence:** medium


## Low (73)

- **[LOW-1]** `agent/claude_brain.py:503` — voice_profile.md opened with relative path — silently skipped on CWD mismatch · *Error handling* · Use Path(__file__).parent.parent / 'memory' / 'voice_profile.md'.
- **[LOW-2]** `agent/claude_brain.py:578` — draft_email_with_claude returns error string instead of raising · *Error handling* · Re-raise after logging.
- **[LOW-3]** `agent/main.py:958` — `mtitle` assigned but never used — pre-call brief title lost · *Correctness* · Either pass `mtitle` into the event dict fallback so the brief uses the resolved title, or remove th
- **[LOW-4]** `agent/scheduler.py:13` — Dead state fields and methods for disabled features · *Amnesia / drift* · Remove the dead attributes and methods, or wrap them in a clearly labelled disabled block.
- **[LOW-5]** `agent/target_roles.py:355` — CTE_EXCLUDE_KEYWORDS overlaps with CTE_RELEVANT_KEYWORDS · *Amnesia / drift* · Document precedence or reorder to evaluate excludes first.
- **[LOW-6]** `gas/Code.gs:602` — slide_count hardcoded to 5 but deck builds 6 base slides · *Amnesia / drift* · Use pres.getSlides().length captured before saveAndClose.
- **[LOW-7]** `gas/Code.gs:665` — testDriveAccess helper with hardcoded folder ID remains in production · *Amnesia / drift* · Remove the function.
- **[LOW-8]** `scripts/ab_analyze.py:69` — Division by zero masked with silent 0% on totals line · *Correctness* · Print warning or 'N/A' when total_v1_cost is zero.
- **[LOW-9]** `scripts/ab_analyze.py:73` — Verdict logic has unreachable final return · *Amnesia / drift* · Add explicit branch for 0-pass/0-fail case.
- **[LOW-10]** `scripts/ab_research_engine.py:384` — _write_record has no exception handling · *Error handling* · Wrap in try/except, fall back to stdout.
- **[LOW-11]** `scripts/audit_c4_prospects.py:133` — Strategy filter hardcoded — silent if column name or value drifts · *State / data integrity* · Add warning if c4 is empty; log distinct Strategy values.
- **[LOW-12]** `scripts/audit_leads_cross_contamination.py:71` — is_generic_source computed but never used · *Amnesia / drift* · Remove or wire into logic.
- **[LOW-13]** `scripts/bug5_dryrun.py:303` — E701 multiple statements on one line confirmed · *Amnesia / drift* · Expand to two lines.
- **[LOW-14]** `scripts/bug5_phase0_scan.py:122` — O(n²) dedup in second pass of clean_sample · *Correctness* · Track seen rows by row number in a set.
- **[LOW-15]** `scripts/bug5_smoke_verify.py:59` — Missing column does not exit non-zero · *Error handling* · Add sys.exit(1) after missing-column print.
- **[LOW-16]** `scripts/create_c4_sequences.py:491` — Sequence descriptions hardcode step counts · *Amnesia / drift* · Generate dynamically via f-string with len(SEQ_*_STEPS).
- **[LOW-17]** `scripts/delete_zzz_sentinels.py:15` — Missing error handling for env var access · *Error handling* · Use os.environ.get with explicit check.
- **[LOW-18]** `scripts/diocesan_drip.py:10` — Docstring says '5 subcommands' but lists 6 · *Amnesia / drift* · Change to '6 subcommands'.
- **[LOW-19]** `scripts/diocesan_drip.py:267` — create_result error check uses 'in' (key presence) not truthiness · *Error handling* · Use create_result.get('error') truthy check.
- **[LOW-20]** `scripts/enrich_c4_pass2.py:412` — active_by_domain initialised but never populated · *State / data integrity* · Populate from Active Accounts or remove.
- **[LOW-21]** `scripts/enrich_c4_titles.py:570` — Redundant enriched-guard in Layer 3 inner loop · *Correctness* · Remove inner guard or document as defensive.
- **[LOW-22]** `scripts/f4_serper_replay.py:305` — argparse subparser setup is dead code — CLI is parsed manually · *Amnesia / drift* · Remove dead argparse or switch fully to argparse.
- **[LOW-23]** `scripts/fetch_csta_roster.py:584` — District resolver makes 2 Serper + 1 Haiku per entry with no cap · *Performance red flags* · Add MAX_RESOLVER_ATTEMPTS cap.
- **[LOW-24]** `scripts/load_campaign.py:209` — diocese_or_group hard-wired to company · *Amnesia / drift* · Check row.get('diocese_or_group') or row.get('diocese') first.
- **[LOW-25]** `scripts/phase1_ground_truth_lackland.py:63` — col_letter silently wraps past column Z · *Correctness* · Add assertion or multi-letter column logic.
- **[LOW-26]** `scripts/phase1b_adjacent_rows.py:62` — abs_num to sheet row mapping undocumented · *Correctness* · Add explicit comment and bounds assert.
- **[LOW-27]** `scripts/phase1c_scramble_block.py:14` — .env parsing doesn't strip inline comments · *Error handling* · Split on ' #' before stripping.
- **[LOW-28]** `scripts/phase1e_executor_test.py:22` — asyncio.get_event_loop() deprecated inside async def · *Concurrency* · Use asyncio.get_running_loop().
- **[LOW-29]** `scripts/phase1g_railway_env_sentinel.py:8` — env_path computed but never used · *Amnesia / drift* · Remove the assignment.
- **[LOW-30]** `scripts/phase2_fingerprint_audit.py:109` — Sample rows display duplicates when fewer than 3 · *Correctness* · Guard len > 3.
- **[LOW-31]** `scripts/phase2b_sample_fingerprints.py:48` — Column letter calculation wraps beyond Z · *Correctness* · Use proper column-to-letter helper.
- **[LOW-32]** `scripts/repair_scrambled_queue_rows.py:78` — fingerprint/extract_fields_from_scrambled pad logic brittle · *Correctness* · Add assertion after padding.
- **[LOW-33]** `scripts/repair_scrambled_queue_rows.py:213` — esa_cluster shares _read_proximity heuristic · *Amnesia / drift* · Verify and split reader if layouts differ.
- **[LOW-34]** `scripts/rule_scanner.py:3` — Module docstring describes only Rule 20 but scanner enforces R19, R20, and R21 · *Amnesia / drift* · Update docstring to reflect all three rules.
- **[LOW-35]** `scripts/rule_scanner.py:314` — main() ignores scan exceptions and always exits 0 · *Error handling* · Wrap main() body in try/except, emit JSON error to stdout.
- **[LOW-36]** `scripts/s59_regen_diocesan_sequences.py:103` — SCHOOLS_URL referenced inside function but not parameterized · *Amnesia / drift* · Add schools_url parameter with SCHOOLS_URL default.
- **[LOW-37]** `scripts/s60_schedule_lookup.py:58` — urllib.parse imported inside loop body · *Amnesia / drift* · Move to top-level imports.
- **[LOW-38]** `scripts/s60_verify_test_targets.py:43` — Private function called directly from script · *Amnesia / drift* · Add public wrapper or use existing public API.
- **[LOW-39]** `scripts/telethon_auth.py:44` — Client left connected if get_me raises after sign_in · *Error handling* · Use async with TelegramClient or try/finally.
- **[LOW-40]** `scripts/test_campaign_file.py:61` — Canary fixture hardcoded expected values drift-prone · *Amnesia / drift* · Add comment or constants at top of file.
- **[LOW-41]** `scripts/test_export_sequence_for_editing.py:21` — load_campaign imported but never used · *Amnesia / drift* · Remove from import, or add TODO if round-trip test was planned.
- **[LOW-42]** `scripts/test_load_campaign.py:503` — main() catches all exceptions uniformly · *Error handling* · Separately classify non-AssertionError exceptions.
- **[LOW-43]** `scripts/test_outreach_validator.py:219` — Hot-lead override test uses cold-cadence schedule_id=52 instead of hot-lead schedule 51 · *Amnesia / drift* · Set kw['schedule_id'] = 51 in the test.
- **[LOW-44]** `scripts/test_role_classifier.py:178` — test_pass_through_mode mutates module-level constant without isolation · *Amnesia / drift* · Wrap in context manager or unittest.mock.patch.
- **[LOW-45]** `scripts/test_rule_scanner.py:14` — No test coverage for label appearing before the number in R20 · *Correctness* · Add test cases where the label root precedes the number.
- **[LOW-46]** `scripts/test_rule_scanner.py:57` — R21 multi-match expected counts undocumented and fragile · *Amnesia / drift* · Add inline comments listing specific pattern names expected to fire.
- **[LOW-47]** `scripts/tg_recent.py:24` — No guard against missing CLI argument; IndexError on sys.argv[1] · *Error handling* · Add a `len(sys.argv) < 2` guard at the top of the `__main__` block that prints usage and exits with 
- **[LOW-48]** `tools/campaign_file.py:310` — step_intervals_days type not validated before use · *Error handling* · Validate type or wrap in try/except.
- **[LOW-49]** `tools/charter_prospector.py:91` — Redundant guard on filter_cmos_by_state call · *Amnesia / drift* · Remove the 'if state:' guard.
- **[LOW-50]** `tools/compliance_gap_scanner.py:462` — Telegram footer says 'scaling to IL/MA' but IL and MA are already in PILOT_STATES · *Amnesia / drift* · Update message to reflect actual exit criterion.
- **[LOW-51]** `tools/contact_extractor.py:20` — _known_contacts_cache never invalidated · *Amnesia / drift* · Expose clear_known_contacts_cache helper and call from research pipeline after writes.
- **[LOW-52]** `tools/contact_extractor.py:57` — AttributeError in _log_usage silently swallowed · *Error handling* · Log a warning when AttributeError is caught.
- **[LOW-53]** `tools/csv_importer.py:754` — replace_accounts_by_state silently no-ops when State column missing · *State / data integrity* · Raise/return early with descriptive error when state_col_idx == -1.
- **[LOW-54]** `tools/daily_call_list.py:41` — Log message reports wrong cap variable · *Correctness* · Include both values.
- **[LOW-55]** `tools/daily_call_list.py:332` — _generate_talking_point uses len(schools) but district_info.school_count may differ · *Amnesia / drift* · Use district_info.get('school_count', len(schools)) canonically.
- **[LOW-56]** `tools/district_prospector.py:3152` — find_lookalike_districts territory bonus misses CA districts · *Amnesia / drift* · Adding 'CA' to `_TERRITORY_STATES` will also fix this automatically.
- **[LOW-57]** `tools/email_drafter.py:451` — clear_processed_cache docstring says 're-enable seeding' but the function explicitly prevents re-seeding · *Amnesia / drift* · Replace the docstring phrase 're-enable seeding' with 'keeps _seeded=True so the next process_new_em
- **[LOW-58]** `tools/gas_bridge.py:137` — CLAUDE.md documents create_draft without skip_if_draft_exists parameter · *Amnesia / drift* · Update tools/CLAUDE.md to include the parameter.
- **[LOW-60]** `tools/outreach_client.py:1013` — _try_fetch silently drops params when passed an empty dict · *Correctness* · Change the condition to `if params is not None`.
- **[LOW-61]** `tools/outreach_client.py:1538` — Dead _EMAIL_LOCAL_RE variable defined via unsatisfiable if-False guard, shadowed by _EMAIL_LOCAL_PART_RE one line later · *Amnesia / drift* · Remove the `_EMAIL_LOCAL_RE = ... if False else None` line entirely.
- **[LOW-63]** `tools/private_schools.py:34` — ENABLE_PRIVATE_SCHOOL_DISCOVERY kill switch has a stale Phase 0 comment referencing BUG 3 investigation · *Amnesia / drift* · Verify whether BUG 3 has been resolved and re-enable the flag, or update the comment to reflect the 
- **[LOW-64]** `tools/private_schools.py:267` — build_csta_enrichment return value unpacked without try/except — inconsistent with signal_processor callers · *Correctness* · Wrap the build_csta_enrichment call in a try/except with a fallback.
- **[LOW-65]** `tools/prospect_loader.py:352` — validate_prospect_inputs imported but never called directly · *Amnesia / drift* · Update docstring to reflect actual one-step flow; remove unused import.
- **[LOW-66]** `tools/proximity_engine.py:292` — Adaptive suggestion uses int() truncation instead of rounding · *Correctness* · Use round() instead of int().
- **[LOW-67]** `tools/research_engine.py:431` — L1 builds 8 queries then silently discards 3 by slicing to `[:5]` · *Correctness* · Pick one limit and apply it consistently.
- **[LOW-68]** `tools/research_engine.py:763` — `asyncio.get_event_loop()` used inside `async def` — deprecated in Python 3.10+ · *Concurrency* · Replace all `asyncio.get_event_loop()` calls inside `async def` methods with `asyncio.get_running_lo
- **[LOW-69]** `tools/role_classifier.py:188` — classified_at timestamp uses naive local time, not UTC · *State / data integrity* · Use datetime.now(timezone.utc) or datetime.utcnow() + 'Z'.
- **[LOW-70]** `tools/sheets_writer.py:253` — cleanup_and_format_sheets banding skip uses name comparison with stale tab_gids · *Amnesia / drift* · Collect GIDs of tabs queued for deletion and use gid comparison.
- **[LOW-71]** `tools/signal_processor.py:3584` — heat_score computed with 'grant' weight but signal_type stored as 'cs_funding_award' · *Correctness* · Store `signal_type="grant"` to match what was used for scoring, or add `"cs_funding_award"` to SIGNA
- **[LOW-72]** `tools/territory_data.py:617` — Genuine exceptions in lookup_district_enrollment logged at INFO · *Error handling* · Log at WARNING or ERROR level; INFO for expected misses only.
- **[LOW-73]** `tools/territory_data.py:735` — Returned service and sheet_id from _ensure_tab captured but abandoned · *Amnesia / drift* · Reuse the captured service across the sync, or use _ for all three values.
- **[LOW-74]** `tools/territory_map.py:230` — Layer color mismatch between comment (yellow) and actual (orange) · *Amnesia / drift* · Update comment and docstring to orange.
- **[LOW-75]** `tools/timezone_lookup.py:95` — Broad except Exception swallows unexpected errors · *Correctness* · Remove bare Exception clause or narrow to expected errors.

## Nits (4)

- **[NIT-1]** `agent/target_roles.py:242` — REGIONAL_ENTITIES TN count is 0 marked 'informal'
- **[NIT-2]** `tools/email_drafter.py:231` — Thread transcript truncation loop over-counts separator for last kept block
- **[NIT-3]** `tools/private_schools.py:229` — time.sleep(0.3) inside synchronous loop may block event loop if called from async context
- **[NIT-4]** `tools/territory_data.py:313` — _ACRONYMS set literal recreated on every _to_title_case call

## Cross-File Drift / Amnesia — Wave 2 (8)

### [CRIT-1] F821: `gas` undefined in /unanswered handler — NameError at runtime ⭐
- **File:** `agent/main.py:1775` @ `339c92c`
- **Category:** correctness
- **Introduced in:** `a974ee7` (no session tag)
- **Evidence:**
  ```python
              result = await loop.run_in_executor(
                  None, activity_tracker.get_unanswered_emails, days, gas, 30)
  ```
- **Why it matters:** The `/unanswered` branch passes `gas` as the second positional arg but `gas` is never assigned anywhere in handle_message before this point. Ruff flags as F821. Python raises `NameError` at runtime.
- **Suggested fix:** Add `gas = get_gas_bridge()` before the `run_in_executor` call with an error guard.
- **Recommended action:** `fix-now` — Any invocation of /unanswered raises NameError and crashes the Telegram handler, making this a live production outage for that command.
- **Confidence:** high
- **Re-eval:** defensible on re-read — The snippet clearly passes `gas` as a positional argument in the /unanswered branch, and there is no assignment of `gas` visible before that point in handle_message. Ruff F821 and the runtime NameError claim are both well-founded.

### [HIGH-9] classify_role reimplemented three times in scripts/ with divergent bucket names vs. tools/role_classifier.py ⭐
- **File:** `scripts/audit_c4_prospects.py:74` @ `339c92c`
- **Category:** amnesia
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
  def classify_role(title: str) -> str:
      """Classify a title into a role bucket."""
      t = title.lower().strip()
      if not t:
          return "unknown"
  ```
- **Why it matters:** Three script versions return different bucket names (library_contact, district_contact, admin, teacher, unknown) from tools/role_classifier.py (admin, curriculum, it, teacher, coach, other). Mixing produces silent misclassifications.
- **Suggested fix:** Rename to `_classify_role_c4` to avoid name collision, or delete and use the tools version.
- **Recommended action:** `fix-now` — Three divergent classify_role implementations produce incompatible bucket names that silently corrupt role-based filtering and reporting across scripts.
- **Confidence:** high
- **Re-eval:** defensible on re-read — Three independent classify_role implementations returning different bucket name sets will produce silent misclassifications when outputs are compared or aggregated — this is a genuine amnesia/drift defect with real downstream data integrity risk.

### [HIGH-18] scripts/create_c4_sequences.py reimplements create_sequence without Session 59 hardening ⭐
- **File:** `scripts/create_c4_sequences.py:89` @ `339c92c`
- **Category:** amnesia
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
  def create_sequence(name, description, steps, tags=None):
      """
      Create a full sequence with steps and templates.
  
      steps: list of dicts, each with:
          - subject: str
          - body: str (plain text, will be converted to HTML)
          - interval: int (seconds)
          - use_existing_template: int or None (template ID to reuse)
      """
  ```
- **Why it matters:** Script defines its own `create_sequence` that bypasses all Session 59 hardening (validate_sequence_inputs, schedule-ID allowlist, min_interval_seconds guard, post-write verify_sequence). Violates Rule 18.
- **Suggested fix:** Replace local create_sequence with direct calls to tools.outreach_client.create_sequence.
- **Confidence:** high

### [HIGH-21] normalize_name in scripts/enrich_c4_pass2.py is a weak stub; tools/csv_importer.normalize_name is authoritative ⭐
- **File:** `scripts/enrich_c4_pass2.py:365` @ `339c92c`
- **Category:** amnesia
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
  def normalize_name(name):
      """Simple name normalization for matching."""
      return re.sub(r"[^a-z0-9 ]", "", (name or "").lower()).strip()
  ```
- **Why it matters:** Scripts version only strips non-alphanumeric. Tools version additionally strips district suffixes (ISD/USD/Unified) and expands abbreviations (LAUSD → los angeles). Mismatches for abbreviated district names.
- **Suggested fix:** Import csv_importer.normalize_name and remove the local stub.
- **Confidence:** high

### [HIGH-40] scripts/s59_regen_diocesan_sequences.py imports _get_service / _get_sheet_id but never uses them ⭐
- **File:** `scripts/s59_regen_diocesan_sequences.py:24` @ `339c92c`
- **Category:** drift
- **Introduced in:** Session 59 (`4051f53`)
- **Evidence:**
  ```python
  from tools.sheets_writer import _get_service, _get_sheet_id
  ```
- **Why it matters:** F401 confirms both imports unused. Private module-level helpers imported but never referenced — signals incomplete write-back work.
- **Suggested fix:** Remove the import line. If write-back is needed, use public sheets_writer API.
- **Confidence:** high

### [MED-31] diocesan_drip.py imports LoadPlan, state_to_timezone, _api_post, _api_get — all unused ⭐
- **File:** `scripts/diocesan_drip.py:50` @ `339c92c`
- **Category:** drift
- **Introduced in:** `fdf6920` (no session tag)
- **Evidence:**
  ```python
  from tools.outreach_client import (  # noqa: E402
      create_prospect,
      add_prospect_to_sequence,
      get_sequence_states,
      _api_post,
      _api_get,
  )
  from tools.prospect_loader import (  # noqa: E402
      Contact,
      LoadPlan,
  ```
- **Why it matters:** All four F401-flagged. Private helpers imported externally. state_to_timezone never called despite docstring claiming timezone enrichment.
- **Suggested fix:** Remove unused imports. If timezone enrichment is needed, add the state_to_timezone call back in the contact processing loop.
- **Confidence:** high

### [MED-38] enrich_c4_titles.py line 623: title_patterns assigned but never used — dead code block ⭐
- **File:** `scripts/enrich_c4_titles.py:623` @ `339c92c`
- **Category:** quality
- **Introduced in:** `353b1f0` (no session tag)
- **Evidence:**
  ```python
      # Quick extraction from search results (look for title in snippets)
      title_patterns = [
          r"(?:^|\W)(teacher|principal|superintendent|director|coordinator|librarian|specialist|coach|instructor|administrator|dean)(?:\W|$)",
      ]
  ```
- **Why it matters:** F841. The regex matching logic that would use this list is absent. Script silently skips an advertised title-extraction step.
- **Suggested fix:** Either implement the regex extraction or remove the dead assignment.
- **Confidence:** high

### [LOW-59] Module-level docstring for tools/outreach_client.py documents interval_minutes but function accepts interval_seconds ⭐
- **File:** `tools/outreach_client.py:14` @ `339c92c`
- **Category:** drift
- **Introduced in:** `3e31a31` (no session tag)
- **Evidence:**
  ```python
    outreach_client.create_sequence(name, steps=[{subject, body_html, interval_minutes}])
  ```
- **Why it matters:** File-level docstring shows deprecated key. Callers reading only the file docstring may pass interval_minutes thinking it's current.
- **Suggested fix:** Update docstring to show interval_seconds as the primary key.
- **Confidence:** high

## Rule-by-Rule Compliance — Wave 3

| Rule | Status | Note |
|------|--------|------|
| Rule 0 — Primary/Secondary Targeting | ⚪ inconclusive | Strategy targeting logic is in agent code prompts; no direct code enforcement. |
| Rule 1 — Plan Mode Before Building | ⚪ inconclusive | Process rule, not code-enforced. |
| Rule 2 — Always push via git, never /push_code | ✅ OK | Delegates to github_pusher.push_file. |
| Rule 7 — New scanners ship with kill switches | ✅ OK | ENABLE_FUNDING_SCAN, ENABLE_COMPETITOR_SCAN, ENABLE_CSTA_SCAN, ENABLE_COMPLIANCE_SCAN, ENABLE_HOMESCHOOL_COOP_DISCOVERY, ENABLE_PRIVATE_SCHOOL_DISCOVERY all present. |
| Rule 8 — Signal vs. Prospect routing | ✅ OK | F9 compliance scanner writes to Signals tab only; promotion via /signal_act. |
| Rule 10 — handle_message() and async tasks must call get_gas_bridge() locally | ✅ OK | All scheduled async functions call get_gas_bridge() into a local variable. |
| Rule 11 — Never use requests or time.sleep() inside async functions | ✅ OK | No async functions use blocking calls directly; sync modules wrapped in run_in_executor. |
| Rule 12 — global declarations at TOP of handle_message() | ✅ OK | Line 1505 is the first statement inside handle_message(). |
| Rule 13 — tool_result always follows tool_use | ✅ OK | Every execute_tool() wrapped in try/except; tool_result appended unconditionally. |
| Rule 14 — Explicit slash commands bypass Claude and call execute_tool() directly | ✅ OK | All documented slash commands confirmed direct-dispatch. |
| Rule 15 — NEVER fabricate claims about active accounts; ALL sequences are drafts | ✅ OK | Upward strategy context explicitly adds 'CRITICAL: Do NOT fabricate claims'; _on_prospect_research_complete marks prospect as 'draft'. |
| Rule 17 — state AND timezone required before create_prospect | ✅ OK | validate_prospect_inputs() enforces non-empty IANA timezone; prospect_loader derives timezone via state_to_timezone. |
| Rule 18 — never write new one-shot Outreach prospect loader | ❌ violation | See _drift.json: scripts/create_c4_sequences.py reimplements create_sequence bypassing all Session 59 hardening. |
| Rule 19 — never show Outreach backend numeric IDs to Steven | ❌ violation | main.py:3843 sends sequence ID directly to Steven via Telegram; outreach_client.py:1994 and 2004 include sequence ID and schedule_id in Google Doc export. |
| Rule 20 — rule_scanner.py structural enforcement | ✅ OK | scripts/rule_scanner.py has R20, R19, and R21 with RULES list and stop hook wiring. |
| Rule 21 — Verify before instructing | ✅ OK | rule_scanner.py R21 has trigger_and_missing_anchor type check. |

### Rule Violations Detail

### [MED-4] Rule 19 violation: Outreach backend sequence ID sent to Steven via Telegram ⭐
- **File:** `agent/main.py:3843` @ `339c92c`
- **Category:** rule_compliance
- **Introduced in:** `873c091` (no session tag)
- **Evidence:**
  ```python
  await send_message(f"Found: **{match['name']}** (ID: {match['id']}). Exporting...")
  ```
- **Why it matters:** The /export_sequence handler sends the raw numeric Outreach sequence ID to Steven via Telegram, violating Rule 19's presentation boundary translation requirement.
- **Suggested fix:** Remove '(ID: {match["id"]})' from the Telegram message.
- **Confidence:** high

### [LOW-62] Rule 19 violation: format_sequence_export exposes backend ID and schedule_id in Google Doc ⭐
- **File:** `tools/outreach_client.py:1994` @ `339c92c`
- **Category:** rule_compliance
- **Introduced in:** `873c091` (no session tag)
- **Evidence:**
  ```python
  lines.append(f"*Exported from Outreach.io — ID: {seq_info['id']}*\n")
  ```
- **Why it matters:** format_sequence_export() writes raw numeric sequence ID and schedule_id (line 2004) into the Google Doc that Steven reviews.
- **Suggested fix:** Remove the ID field and translate schedule_id to its human name using the schedule_id map.
- **Confidence:** high

## Session Attribution

| Session | Finding count |
|---------|---------------|
| 60 | 6 |
| 59 | 2 |

_Session attribution is extremely sparse — only 8 of 270 findings carry session tags (6 attributed to session 60, 2 to session 59), so no meaningful per-session blame pattern can be established from this audit run._

## Needs Manual Review (2)

_Findings where the byte-match verification step failed — most likely whitespace/quoting differences between the LLM's quoted snippet and the actual file. Not necessarily invalid; read and verify manually._

- **MEDIUM** `agent/main.py:1505` — `global _pending_draft` declared in handle_message but never assigned there · verify_reason: `first line not within 1505+-20`
  - Snippet: `async def execute_tool(tool_name: str, tool_input: dict) -> str:
    global _pending_draft`
- **HIGH** `scripts/audit_c4_prospects.py:129` — Row padding bug — '"" * N' always yields '' and is wrapped in single-element list · verify_reason: `snippet not a substring`
  - Snippet: `        padded = row + ["" * (len(headers) - len(row))]
`

## Coverage (69/69)

_All 69 post-S40 target files were audited. Three initially dispatched agents (tools/sequence_builder.py, tools/pipeline_tracker.py, tools/call_processor.py) returned no-prompt errors because those files were not in the post-S40 diff — they pre-date S40 and are correctly outside the audit scope._

| File | Status |
|------|--------|
| `agent/claude_brain.py` | audited |
| `agent/main.py` | audited |
| `agent/scheduler.py` | audited |
| `agent/target_roles.py` | audited |
| `gas/Code.gs` | audited |
| `scripts/ab_analyze.py` | audited |
| `scripts/ab_research_engine.py` | audited |
| `scripts/audit_c4_prospects.py` | audited |
| `scripts/audit_leads_cross_contamination.py` | audited |
| `scripts/bug5_cleanup_lackland_test.py` | audited |
| `scripts/bug5_dryrun.py` | audited |
| `scripts/bug5_phase0_scan.py` | audited |
| `scripts/bug5_smoke_verify.py` | audited |
| `scripts/create_c4_sequences.py` | audited |
| `scripts/delete_zzz_sentinels.py` | audited |
| `scripts/diocesan_drip.py` | audited |
| `scripts/enrich_c4_pass2.py` | audited |
| `scripts/enrich_c4_titles.py` | audited |
| `scripts/f4_serper_replay.py` | audited |
| `scripts/fetch_csta_roster.py` | audited |
| `scripts/level2_integration_check.py` | audited |
| `scripts/load_campaign.py` | audited |
| `scripts/phase1_ground_truth_lackland.py` | audited |
| `scripts/phase1b_adjacent_rows.py` | audited |
| `scripts/phase1c_scramble_block.py` | audited |
| `scripts/phase1d_local_sentinel.py` | audited |
| `scripts/phase1e_executor_test.py` | audited |
| `scripts/phase1g_railway_env_sentinel.py` | audited |
| `scripts/phase2_fingerprint_audit.py` | audited |
| `scripts/phase2b_sample_fingerprints.py` | audited |
| `scripts/repair_scrambled_queue_rows.py` | audited |
| `scripts/rule_scanner.py` | audited |
| `scripts/s59_regen_diocesan_sequences.py` | audited |
| `scripts/s60_schedule_lookup.py` | audited |
| `scripts/s60_test_target_lookup.py` | audited |
| `scripts/s60_verify_test_targets.py` | audited |
| `scripts/telethon_auth.py` | audited |
| `scripts/test_campaign_file.py` | audited |
| `scripts/test_diocesan_drip.py` | audited |
| `scripts/test_export_sequence_for_editing.py` | audited |
| `scripts/test_load_campaign.py` | audited |
| `scripts/test_outreach_validator.py` | audited |
| `scripts/test_role_classifier.py` | audited |
| `scripts/test_round1_unit.py` | audited |
| `scripts/test_rule_scanner.py` | audited |
| `scripts/tg_recent.py` | audited |
| `scripts/tg_send.py` | audited |
| `tools/activity_tracker.py` | audited |
| `tools/campaign_file.py` | audited |
| `tools/charter_prospector.py` | audited |
| `tools/compliance_gap_scanner.py` | audited |
| `tools/contact_extractor.py` | audited |
| `tools/csv_importer.py` | audited |
| `tools/cte_prospector.py` | audited |
| `tools/daily_call_list.py` | audited |
| `tools/district_prospector.py` | audited |
| `tools/email_drafter.py` | audited |
| `tools/gas_bridge.py` | audited |
| `tools/outreach_client.py` | audited |
| `tools/private_schools.py` | audited |
| `tools/prospect_loader.py` | audited |
| `tools/proximity_engine.py` | audited |
| `tools/research_engine.py` | audited |
| `tools/role_classifier.py` | audited |
| `tools/sheets_writer.py` | audited |
| `tools/signal_processor.py` | audited |
| `tools/territory_data.py` | audited |
| `tools/territory_map.py` | audited |
| `tools/timezone_lookup.py` | audited |

## Static Tool Summary

**Ruff** — 367 total hits

| Rule | Count |
|------|-------|
| F541 | 132 |
| E402 | 82 |
| F401 | 60 |
| E741 | 32 |
| F841 | 25 |
| E401 | 16 |
| E701 | 8 |
| F601 | 7 |
| F821 | 2 |
| E731 | 2 |
| E713 | 1 |

**Pyflakes** — 207 hits (also surfaced as ruff F*)

**Bandit** — 172 total hits

| Rule | Count |
|------|-------|
| B101 | 109 |
| B110 | 27 |
| B108 | 17 |
| B105 | 4 |
| B310 | 4 |
| B324 | 3 |
| B311 | 3 |
| B112 | 2 |
| B104 | 1 |
| B102 | 1 |
| B501 | 1 |

**Compileall** — clean (all 69 files parse without error)

**Vulture** — 6 hits (min-confidence=80)

## TODO / FIXME Dump (16 matches)

```
tools/todo_manager.py:34:TAB_TODOS = "Todo List"
tools/todo_manager.py:36:TODO_COLUMNS = [
tools/todo_manager.py:77:    if TAB_TODOS not in existing:
tools/todo_manager.py:80:            body={"requests": [{"addSheet": {"properties": {"title": TAB_TODOS}}}]}
tools/todo_manager.py:82:        logger.info(f"Created tab: {TAB_TODOS}")
tools/todo_manager.py:86:        range=f"'{TAB_TODOS}'!A1",
tools/todo_manager.py:88:        body={"values": [TODO_COLUMNS]}
tools/todo_manager.py:99:        range=f"'{TAB_TODOS}'!A:G",
tools/todo_manager.py:130:        range=f"'{TAB_TODOS}'!A2:G10000",
tools/todo_manager.py:138:        rows.append([item.get(col, "") for col in TODO_COLUMNS])
tools/todo_manager.py:142:        range=f"'{TAB_TODOS}'!A2",
tools/district_prospector.py:81:    "Signal ID",        # SIG-XXX from Signals tab (for attribution tracking)
tools/signal_processor.py:2997:  "dollar_amount": "$X,XXX or empty string if unknown",
tools/signal_processor.py:3143:  "amount": "$X,XXX or empty if unknown",
tools/signal_processor.py:4139:  "dollar_amount": "$X,XXX or empty string if unknown",
scripts/test_hook_wrappers.sh:46:TMP_LOG="$(mktemp -t scout-smoke-vlog.XXXXXX)"
```

## Provenance

- **RUN_ID:** 2026-04-15_0128
- **HEAD:** `339c92c`
- **S40 baseline:** `629a2b5` (2026-04-02 20:23:30 -0500)
- **Start:** 2026-04-15 01:28 CDT
- **End:** 2026-04-15 02:15 CDT (approximate)
- **Subagent calls:** ~74 (1 smoke + 68 Wave 1 + 1 Wave 2 drift + 1 Wave 3 rules + 1 Wave 4c synthesis + 2 out-of-scope errors)
- **Verified / rejected:** 270 / 2
- **Total unique findings:** 270
- **Severity totals:** CRITICAL=2, HIGH=72, MEDIUM=117, LOW=75, NIT=4
- **Mode:** Tier 1 (ephemeral venv at `/tmp/scout-audit/2026-04-15_0128/audit-venv`)
- **LLM:** Claude Sonnet 4.6 (Explore agent type) throughout
- **Cost (estimated):** ~$5

_Ephemeral state: `/tmp/scout-audit/2026-04-15_0128/`_
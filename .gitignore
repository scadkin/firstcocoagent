# Environment variables (secrets — never commit)
.env

# Raw terminal captures (cleaned versions are in docs/sessions/)
docs/sessions/.raw/

# Python virtual environment
.venv/

# Evaluation results (local-only, regenerable)
scripts/eval_results/

# Python bytecode cache (any subdirectory)
**/__pycache__/
.telethon_session*
